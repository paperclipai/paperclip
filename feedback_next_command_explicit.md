# next 지시어 명확 제시

완료 보고 끝에는 다음에 실행할 명령어를 구체적으로 제시한다.

## 규칙

- 다음 단계가 GSD 흐름이면 `$gsd-*`가 아니라 `/gsd-*` 슬래시 커맨드로 쓴다.
- 사용자가 `--auto --chain` 흐름을 원한 경우, 다음 명령어에도 `--auto --chain`을 포함한다.
- “다음은 진행하면 됩니다”처럼 추상적으로 끝내지 않는다.
- 가능한 경우 한 줄짜리 실행 명령으로 끝낸다.

## 예시

```sh
/gsd-next --auto --chain
```
