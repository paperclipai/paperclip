numpy>=1.26
sounddevice>=0.4.6
# openwakeword zieht onnxruntime als Backend automatisch mit. ONNX statt tflite,
# weil `tflite_runtime` auf macOS arm64 kein Wheel hat (tensorflow liefert das
# von openwakeword erwartete `tflite_runtime` NICHT).
openwakeword>=0.6.0
