# TODO

- [ ] Build a native `lmstudio_local` adapter for Paperclip (model discovery via LM Studio API, environment test, and execution path), so users are no longer limited by `opencode models` provider filtering.
