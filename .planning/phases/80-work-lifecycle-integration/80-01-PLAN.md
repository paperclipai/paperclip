---
phase: 80-work-lifecycle-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/rt2_v33_domain_events.ts
  - packages/db/src/schema/rt2_v33_execution_attempts.ts
  - packages/shared/src/types/rt2-domain-events.ts
  - packages/shared/src/validators/rt2-domain-events.ts
  - server/src/services/rt2-domain-events.ts
  - server/src/services/rt2-task-execution.ts
  - server/src/services/rt2-task-engine.ts
autonomous: true
requirements:
  - RT2-01
  - RT2-02
  - RT2-03
must_haves:
  truths:
    - "RT2-01: `rt2_v33_domain_events` records are NEVER updated or deleted — no UPDATE/DELETE paths exist in RT2 service code; `appendAndProject` is the only write path"
    - "RT2-02: Full execution lifecycle (dispatch → start → complete/fail/cancel) emits domain events consumed via `listTimeline`; all required dispatch fields (executorType, executorId, runtimeServiceId, heartbeatRunId, executionWorkspaceId) are set atomically"
    - "RT2-03: `rt2TaskEngineService` is the ONLY task creation path; all 7 RT2-native operation events (task.created, todo.created, deliverable.defined, participant.joined/assigned/ended, task.capacity_changed, todo.started) are confirmed; zero legacy Paperclip patterns (WorkQueue, AgentTask, blocked-as-runtime-target) exist in RT2 surfaces"
  artifacts:
    - path: packages/db/src/schema/rt2_v33_domain_events.ts
      provides: Append-only domain event table with idempotency key unique index
      exports: rt2V33DomainEvents, companyIdempotencyUq
    - path: server/src/services/rt2-domain-events.ts
      provides: Canonical single write path for all RT2 domain events
      exports: appendAndProject (line 279)
    - path: server/src/services/rt2-task-execution.ts
      provides: Full execution lifecycle with atomic dispatch field assignment and idempotency-keyed events
      exports: dispatch, start, complete, fail, cancel, cleanupStale, listTimeline, appendExecutionEvent
    - path: server/src/services/rt2-task-engine.ts
      provides: RT2-native task/todo/participant/deliverable CRUD with domain events
      exports: createTask, createTodo, addParticipant, updateCapacity, endParticipant, startTodo
  key_links:
    - from: server/src/services/rt2-task-execution.ts:dispatch
      to: server/src/services/rt2-domain-events.ts:appendAndProject
      via: await appendExecutionEvent dispatches with idempotency key rt2.execution.dispatched:${attemptId}
      pattern: "appendExecutionEvent.*appendAndProject.*rt2\\.execution\\.dispatched"
    - from: server/src/services/rt2-task-engine.ts:createTask
      to: packages/db/src/schema/rt2_v33_domain_events.ts
      via: txEvents.appendAndProject emits rt2.task.created + rt2.deliverable.defined atomically
      pattern: "rt2\\.task\\.created.*rt2\\.deliverable\\.defined.*createTask"
---

<objective>
Verify RT2 Work Lifecycle Integration for RT2-01, RT2-02, RT2-03. Phase 79 established baseline proof for append-only event stream, execution lifecycle events, and RT2-native operation contracts. Phase 80 extends to deeper verification of the write-path isolation (proving no UPDATE/DELETE on rt2_v33_domain_events), full lifecycle integration end-to-end, and legacy Paperclip pattern scanning. This phase does NOT implement new features — it verifies existing code against the decisions in 80-CONTEXT.md.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-work-lifecycle-integration/80-CONTEXT.md
@.planning/phases/79-rt2-event-projector-alignment/79-CONTEXT.md
@.planning/phases/79-rt2-event-projector-alignment/79-01-SUMMARY.md
@packages/db/src/schema/rt2_v33_domain_events.ts
@packages/db/src/schema/rt2_v33_execution_attempts.ts
@server/src/services/rt2-domain-events.ts
@server/src/services/rt2-task-execution.ts
@server/src/services/rt2-task-engine.ts
@packages/shared/src/types/rt2-domain-events.ts
@packages/shared/src/validators/rt2-domain-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RT2-01 — Append-Only Guarantee (Deeper Verification)</name>
  <files>packages/db/src/schema/rt2_v33_domain_events.ts, packages/db/src/schema/rt2_v33_execution_attempts.ts, server/src/services/rt2-domain-events.ts, server/src/services/rt2-task-execution.ts, server/src/services/rt2-task-engine.ts</files>
  <read_first>
packages/db/src/schema/rt2_v33_domain_events.ts
server/src/services/rt2-domain-events.ts
server/src/services/rt2-task-execution.ts
server/src/services/rt2-task-engine.ts
  </read_first>
  <action>
**Deeper verification of RT2-01: Prove NO UPDATE/DELETE paths on `rt2_v33_domain_events` in RT2 service code.**

Phase 79 verified the baseline (idempotency key unique index, appendAndProject is the write path). Phase 80 goes deeper:

1. **Schema scan — confirm append-only semantics:**
   Grep `packages/db/src/schema/rt2_v33_domain_events.ts` to confirm:
   - `companyIdempotencyUq` unique index at line 44-46 with WHERE clause on `idempotencyKey IS NOT NULL`
   - No `onUpdate`, `onDelete` cascade rules on the table (line 5-47) — table is insert-only

2. **RT2 service code scan — prove no UPDATE/DELETE:**
   Grep `server/src/services/rt2-task-execution.ts` for any UPDATE or DELETE operations on `rt2_v33_domain_events`:
   - grep for `.update()` targeting `rt2_v33_domain_events`
   - grep for `.delete()` targeting `rt2_v33_domain_events`
   - grep for `sql\`.*UPDATE.*rt2_v33` or `sql\`.*DELETE.*rt2_v33`
   - All state transitions in dispatch/start/complete/fail/cancel/cleanupStale use `appendExecutionEvent` (domain event append), NOT direct UPDATE on the events table

3. **RT2 task engine scan — prove no UPDATE/DELETE:**
   Grep `server/src/services/rt2-task-engine.ts` for UPDATE/DELETE on `rt2_v33_domain_events`:
   - All mutations (createTask, createTodo, addParticipant, etc.) use `appendAndProject` for domain events
   - No direct SQL UPDATE or DELETE on event records

4. **Domain events service scan:**
   Grep `server/src/services/rt2-domain-events.ts` for appendAndProject implementation:
   - `append` at line 93 is the ONLY write operation (INSERT)
   - No UPDATE or DELETE operations in the file

5. **Verify idempotency keys on all event emission sites:**
   From Phase 79 summary, all 8 execution lifecycle events have idempotency keys. Confirm Phase 80 extends to confirming:
   - `rt2.task.created` → idempotency key `rt2.task.created:${issue.id}`
   - `rt2.todo.created` → `rt2.todo.created:${todo.id}`
   - `rt2.deliverable.defined` → `rt2.deliverable.defined:${deliverable.id}`
   - `rt2.participant.joined/assigned` → `rt2.participant.${mutation}:${participant.id}`
   - `rt2.participant.ended` → `rt2.participant.ended:${taskIssueId}:${userId}:${Date.now()}`
   - `rt2.task.capacity_changed` → `rt2.task.capacity_changed:${taskIssueId}:${Date.now()}`
   - `rt2.todo.started` → `rt2.todo.started:${todo.id}`

6. **Timeline ordering deterministic on replay:**
   Grep `server/src/services/rt2-task-execution.ts` `buildTimelineEvents` (line 228-258) to confirm:
   - Domain events sorted by `occurredAt ASC, createdAt ASC`
   - Heartbeat events sorted by `seq ASC, createdAt ASC`
   - Merge by `createdAt ASC, seq ASC` tiebreaker
   - This produces deterministic ordering on replay
  </action>
  <verify>
    <automated>
      grep -c "companyIdempotencyUq" packages/db/src/schema/rt2_v33_domain_events.ts &&
      grep -c "appendAndProject" server/src/services/rt2-domain-events.ts &&
      (grep -c "rt2_v33_domain_events.*update\|\.update.*rt2_v33_domain_events" server/src/services/rt2-task-execution.ts server/src/services/rt2-task-engine.ts || echo "0") &&
      (grep -c "rt2_v33_domain_events.*delete\|\.delete.*rt2_v33_domain_events" server/src/services/rt2-task-execution.ts server/src/services/rt2-task-engine.ts || echo "0")
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms companyIdempotencyUq unique index exists in rt2_v33_domain_events.ts
    - grep confirms appendAndProject is the ONLY write path in rt2-domain-events.ts
    - grep confirms ZERO UPDATE or DELETE operations on rt2_v33_domain_events in rt2-task-execution.ts and rt2-task-engine.ts
    - grep confirms all 7 RT2-native operation events have idempotency keys
    - grep confirms buildTimelineEvents uses occurredAt/createdAt for domain + seq/createdAt for heartbeat with correct merge sort
  </acceptance_criteria>
  <done>RT2-01 append-only guarantee verified: no UPDATE/DELETE on rt2_v33_domain_events; appendAndProject is the single write path; all events have idempotency keys</done>
</task>

<task type="auto">
  <name>Task 2: Verify RT2-02 — Full Execution Lifecycle Integration</name>
  <files>server/src/services/rt2-task-execution.ts, packages/shared/src/types/rt2-domain-events.ts</files>
  <read_first>
server/src/services/rt2-task-execution.ts
server/src/services/rt2-domain-events.ts
packages/shared/src/types/rt2-domain-events.ts
  </read_first>
  <action>
**Verify RT2-02: Full execution lifecycle (dispatch → start → complete/fail/cancel) works through the event stream.**

This extends Phase 79's per-event verification to prove the integration chain end-to-end.

1. **Verify dispatch sets ALL required fields atomically (D-09 from 80-CONTEXT):**
   Grep `server/src/services/rt2-task-execution.ts` for `dispatch()` implementation:
   - Line 364-404: confirms `state: "dispatched"` (not "claimed")
   - Confirms all required fields are set: `executorType`, `executorId`, `runtimeServiceId`, `heartbeatRunId`, `executionWorkspaceId`, `claimedAt`
   - Grep for `assertRuntimeCanAccept` call at line 380 (runtime health/capacity check before dispatch)

2. **Verify start accepts both dispatched and claimed (D-10):**
   Grep `start()` implementation (line ~494):
   - `startableStates = ["dispatched", "claimed"]` at line 34
   - Confirms `claimed` attempts can still start for backward compat

3. **Verify cancel emits `rt2.execution.cancelled` with idempotency key (D-11):**
   Grep `cancel()` implementation (line ~572):
   - Transition path: `["queued", "dispatched", "claimed", "running"]` → `cancelled`
   - Idempotency key: `rt2.execution.cancelled:${attemptId}`
   - Confirms domain event emission via `appendExecutionEvent`

4. **Verify cleanupStale emits `rt2.execution.stale_cleaned` (D-12):**
   Grep `cleanupStale()` implementation (line ~598):
   - Transitions stale `dispatched`/`claimed`/`running` → `failed` with reason `"stale_runtime_cleanup"`
   - Default stale window confirmed as 30 minutes
   - Idempotency key: `rt2.execution.stale_cleaned:${candidate.id}:${staleBefore.toISOString()}`

5. **Verify all 8 lifecycle events are consumed by listTimeline:**
   Grep `listTimeline` implementation (line ~652):
   - `buildTimelineEvents` reads from `rt2_v33_domain_events` (line 232-238) + `heartbeat_run_events` (line 242-248) when heartbeatRunId exists
   - Confirms all 8 event types appear in timeline: enqueued, dispatched, started, completed, failed, cancelled, stale_cleaned, retried

6. **Verify normalizeExecutionState is applied everywhere (D-01/D-03 from Phase 67 carry-forward):**
   Grep for `normalizeExecutionState` calls:
   - `toExecutionSummary` at line 40-63 uses it
   - Both `rt2-task-execution.ts` (line 36) and `rt2-task-engine.ts` (line 206) define it
   - Product surfaces never see "claimed" — only "dispatched"
  </action>
  <verify>
    <automated>
      grep -c "assertRuntimeCanAccept" server/src/services/rt2-task-execution.ts &&
      grep -c "startableStates.*dispatched.*claimed" server/src/services/rt2-task-execution.ts &&
      grep -c "stale_runtime_cleanup" server/src/services/rt2-task-execution.ts &&
      grep -c "normalizeExecutionState" server/src/services/rt2-task-execution.ts &&
      grep -c "normalizeExecutionState" server/src/services/rt2-task-engine.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms dispatch() sets state="dispatched" (not "claimed") with all required fields
    - grep confirms assertRuntimeCanAccept is called before dispatch
    - grep confirms startableStates = ["dispatched", "claimed"] for backward compat
    - grep confirms cancel emits rt2.execution.cancelled with idempotency key
    - grep confirms cleanupStale emits rt2.execution.stale_cleaned with correct idempotency
    - grep confirms listTimeline reads from BOTH rt2_v33_domain_events AND heartbeat_run_events
    - grep confirms normalizeExecutionState in both task-execution.ts and task-engine.ts
  </acceptance_criteria>
  <done>RT2-02 execution lifecycle integration verified end-to-end: dispatch sets all required fields atomically, cancel/cleanupStale emit domain events, listTimeline consumes all lifecycle events</done>
</task>

<task type="auto">
  <name>Task 3: Verify RT2-03 — RT2-Native Contracts and Zero Legacy Patterns</name>
  <files>server/src/services/rt2-task-engine.ts, server/src/services/rt2-task-execution.ts, packages/shared/src/types/rt2-domain-events.ts, packages/shared/src/validators/rt2-domain-events.ts</files>
  <read_first>
server/src/services/rt2-task-engine.ts
server/src/services/rt2-task-execution.ts
packages/shared/src/types/rt2-domain-events.ts
packages/shared/src/validators/rt2-domain-events.ts
  </read_first>
  <action>
**Verify RT2-03: Work/Task/Deliverable lifecycle uses RT2-native contracts with zero legacy Paperclip patterns.**

This extends Phase 79's RT2-native contract verification to:
- Prove rt2TaskEngineService.createTask is the ONLY task creation path (D-06)
- Prove zero legacy Paperclip patterns in RT2 service names, types, routes (D-07)
- Prove `blocked` is not a runtime transition target (D-08, D-15)
- Prove all CRUD methods emit domain events — no silent mutations (D-13)

1. **Verify createTask is the ONLY task creation path (D-06):**
   Grep `server/src/services/rt2-task-engine.ts` for `createTask`:
   - Line ~635-680: creates issue + task profile + deliverables + `rt2.task.created` + `rt2.deliverable.defined` in one atomic transaction
   - Confirm no other path creates RT2 tasks (grep for `createIssue` or equivalent in RT2 services)

2. **Verify all 7 RT2-native operation events exist (D-05):**
   Grep `server/src/services/rt2-task-engine.ts` for each event type:
   - `rt2.task.created` (line ~648)
   - `rt2.todo.created` (line ~775)
   - `rt2.deliverable.defined` (line ~668, ~793)
   - `rt2.participant.joined` / `rt2.participant.assigned` (line ~404)
   - `rt2.participant.ended` (line ~900)
   - `rt2.task.capacity_changed` (line ~859)
   - `rt2.todo.started` (line ~953)

3. **Scan for legacy Paperclip patterns in RT2 surfaces (D-07):**
   Grep all RT2 service files for legacy patterns:
   - `WorkQueue`, `AgentTask`, `createIssue` (Paperclip legacy naming)
   - `rt2_v33_execution_attempts` CHECK constraint for blocked state (line 10)
   - RT2 services should NOT use legacy Paperclip naming
   - RT2 types should NOT reference Paperclip contract types

4. **Verify `blocked` is NOT a runtime target state (D-08, D-15):**
   Grep `server/src/services/rt2-task-execution.ts`:
   - `runtimeActiveStates = ["dispatched", "claimed", "running"]` at line 33 — `blocked` is NOT included
   - `cancel()` transitions from `["queued", ...runtimeActiveStates]` — not blocked
   - `startableStates = ["dispatched", "claimed"]` — not blocked
   - `blocked` may appear in rt2_v33_execution_attempts schema CHECK constraint for data compatibility, but NOT as a runtime transition target

5. **Verify NO silent mutations (D-13):**
   Grep `rt2-task-engine.ts` for CRUD methods:
   - `createTask`, `createTodo`, `updateTodo`, `addParticipant`, `updateCapacity`, `endParticipant`, `startTodo`
   - Each must call `appendAndProject` / `appendDomainEvent` for the corresponding domain event
   - No method mutates without emitting an event

6. **Verify deliverable lifecycle is event-sourced (D-14):**
   - `rt2.deliverable.defined` is the canonical creation event
   - No direct deliverable table UPDATE operations without domain event emission
  </action>
  <verify>
    <automated>
      grep -E "rt2\\.(task|todo|deliverable|participant)\\.(created|defined|joined|assigned|ended|started|capacity_changed)" server/src/services/rt2-task-engine.ts | wc -l &&
      grep -c "rt2-task-engine" server/src/services/rt2-task-engine.ts &&
      grep -c "rt2-task-execution" server/src/services/rt2-task-execution.ts &&
      (grep -c "WorkQueue\|AgentTask\|createIssue" server/src/services/rt2-task-engine.ts server/src/services/rt2-task-execution.ts packages/shared/src/types/rt2-domain-events.ts || echo "0") &&
      grep -c "runtimeActiveStates.*dispatched.*claimed.*running" server/src/services/rt2-task-execution.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms createTask is the only task creation path (atomic transaction with domain event)
    - grep confirms all 7 RT2-native operation events in rt2-task-engine.ts
    - grep confirms ZERO instances of WorkQueue, AgentTask, or createIssue in RT2 surfaces
    - grep confirms runtimeActiveStates does NOT include "blocked"
    - grep confirms startableStates does NOT include "blocked"
    - grep confirms all CRUD methods in rt2-task-engine.ts emit domain events (no silent mutations)
  </acceptance_criteria>
  <done>RT2-03 RT2-native operation contracts verified: createTask is only task creation path; all 7 RT2-native events confirmed; zero legacy Paperclip patterns; blocked not a runtime target</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime → Database | Runtime service writes state changes atomically with domain events |
| API → appendAndProject | Untrusted input crosses at AppendRt2DomainEvent schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | rt2_v33_domain_events | mitigate | Append-only table with idempotency key unique index (companyIdempotencyUq); no UPDATE/DELETE paths in RT2 service code; append() checks existing before insert (replay-safe) |
| T-80-02 | Denial | listTimeline | accept | Timeline ordering is deterministic (occurredAt ASC + createdAt ASC + seq ASC); concurrent reads are read-only, no race condition |
| T-80-03 | Information | rt2-task-engine.ts CRUD | mitigate | All mutations emit domain events via appendAndProject — no silent mutations; RT2-native contract types validated by appendRt2DomainEventSchema |
| T-80-04 | Repudiation | rt2-task-execution.ts dispatch | mitigate | All dispatch fields set atomically in single transaction; cancel/cleanupStale emit domain events with idempotency keys providing audit trail |
</threat_model>

<verification>
Run the following in sequence:

```bash
# 1. TypeScript type check
pnpm typecheck

# 2. Unit tests for RT2 task and domain events
pnpm exec vitest run packages/shared/src/rt2-task.test.ts
pnpm exec vitest run packages/shared/src/rt2-domain-events.test.ts
pnpm exec vitest run server/src/__tests__/rt2-domain-events.test.ts
```
</verification>

<success_criteria>
- [ ] grep confirms ZERO UPDATE or DELETE on rt2_v33_domain_events in RT2 service code
- [ ] grep confirms appendAndProject is the single write path (only INSERT operations)
- [ ] grep confirms all 7 RT2-native operation events have idempotency keys in rt2-task-engine.ts
- [ ] grep confirms dispatch() sets state="dispatched" with all required fields (executorType, executorId, runtimeServiceId, heartbeatRunId, executionWorkspaceId, claimedAt)
- [ ] grep confirms assertRuntimeCanAccept is called before dispatch
- [ ] grep confirms runtimeActiveStates = ["dispatched", "claimed", "running"] (blocked NOT included)
- [ ] grep confirms startableStates = ["dispatched", "claimed"] (blocked NOT included)
- [ ] grep confirms ZERO legacy Paperclip patterns (WorkQueue, AgentTask, createIssue) in RT2 services
- [ ] grep confirms createTask is the only task creation path (atomic transaction with domain event emission)
- [ ] pnpm typecheck passes
- [ ] pnpm exec vitest run packages/shared/src/rt2-task.test.ts passes
</success_criteria>

<output>
After completion, create `.planning/phases/80-work-lifecycle-integration/80-01-SUMMARY.md`
</output>