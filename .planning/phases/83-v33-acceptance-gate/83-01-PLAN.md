---
phase: 83-v33-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - "GATE-01: A deterministic gate script `scripts/rt2-v33-acceptance-gate.mjs` is created as the v3.3 acceptance wrapper."
    - "GATE-02: The script executes `rt2-devplan-alignment-gate.mjs`, writes `summary.json` and `report.md` to `.planning/v33-acceptance-runs/`, capturing baselineScorePct, currentScorePct, and scoreDeltaPct."
    - "GATE-03: Score delta must be positive vs v3.2 baseline. A failed check is a blocker, not accepted debt."
  artifacts:
    - path: scripts/rt2-v33-acceptance-gate.mjs
      provides: v3.3 acceptance wrapper script
      exports: default execution
    - path: .planning/v33-acceptance-runs/
      provides: Directory for acceptance run outputs
      exports: summary.json, report.md
  key_links:
    - from: scripts/rt2-v33-acceptance-gate.mjs
      to: scripts/rt2-devplan-alignment-gate.mjs
      via: Invokes the alignment gate and analyzes output
      pattern: "rt2-devplan-alignment-gate"
---

<objective>
Close the v3.3 (RT2 Engine Convergence) milestone by verifying that the core engine convergence meets acceptance criteria. Create and run the `rt2-v33-acceptance-gate.mjs` wrapper to ensure DevPlan alignment score hasn't regressed compared to v3.2 baseline, and all standard tests pass.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-v33-acceptance-gate/83-CONTEXT.md
@.planning/phases/83-v33-acceptance-gate/83-DISCUSSION-LOG.md
@.planning/phases/83-v33-acceptance-gate/83-HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 — Implement v3.3 Acceptance Gate Script</name>
  <files>scripts/rt2-v33-acceptance-gate.mjs</files>
  <read_first>
scripts/rt2-devplan-alignment-gate.mjs
  </read_first>
  <action>
**Implement GATE-01: Create the acceptance script.**

1. **Create Script:**
   Create `scripts/rt2-v33-acceptance-gate.mjs`.

2. **Core Execution:**
   The script must:
   - Execute `node scripts/rt2-devplan-alignment-gate.mjs`.
   - Execute `pnpm typecheck`.
   - Execute `pnpm test`.

3. **Output Generation:**
   - Create `.planning/v33-acceptance-runs/` directory if it does not exist.
   - Parse the alignment score output.
   - Calculate baseline vs current score (baseline is the v3.2 state, which can be hardcoded or retrieved).
   - Write `summary.json` containing `baselineScorePct`, `currentScorePct`, and `scoreDeltaPct`.
   - Write a `report.md` with human-readable results.

4. **Failure Conditions:**
   - Any command failure (`typecheck`, `test`, or the alignment gate) is an absolute blocker and the script should exit with code 1.
   - If `scoreDeltaPct` is not positive (i.e. <= 0), fail with a blocker message.
  </action>
  <verify>
    <automated>
      test -f scripts/rt2-v33-acceptance-gate.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/rt2-v33-acceptance-gate.mjs is successfully created.
    - Script explicitly runs alignment gate, typecheck, and test.
    - Script enforces a positive scoreDeltaPct requirement.
  </acceptance_criteria>
  <done>GATE-01 implemented: Acceptance script created with all required verifications.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-02 — Execute Acceptance Gate and Generate Artifacts</name>
  <files>.planning/v33-acceptance-runs/summary.json, .planning/v33-acceptance-runs/report.md</files>
  <read_first>
scripts/rt2-v33-acceptance-gate.mjs
  </read_first>
  <action>
**Execute GATE-02: Run the gate and confirm passing matrix.**

1. **Execute Gate:**
   Run `node scripts/rt2-v33-acceptance-gate.mjs`.

2. **Verify Output:**
   Ensure `.planning/v33-acceptance-runs/summary.json` and `.planning/v33-acceptance-runs/report.md` are generated successfully.

3. **Validate Alignment Row:**
   The alignment gate row for `v33-acceptance-gate` should be marked `complete` based on this execution.
  </action>
  <verify>
    <automated>
      node scripts/rt2-v33-acceptance-gate.mjs &&
      test -f .planning/v33-acceptance-runs/summary.json &&
      test -f .planning/v33-acceptance-runs/report.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Gate script runs to completion with exit code 0.
    - Output artifacts summary.json and report.md are generated.
    - Score delta is positive and no verification checks fail.
  </acceptance_criteria>
  <done>GATE-02 executed: Gate run successful, v3.3 acceptance criteria met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Acceptance Gate -> CI | Final milestone verification must be deterministic and fully trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | Gate Scripts | mitigate | Gate logic relies on strict exit codes and matrix constraints that fail closed. |
| T-83-02 | Information Disclosure | summary.json | ignore | Test outputs are public within repo context. |
</threat_model>

<verification>
Run the acceptance gate:

```bash
node scripts/rt2-v33-acceptance-gate.mjs
```
</verification>

<success_criteria>
- [ ] scripts/rt2-v33-acceptance-gate.mjs is created and operational.
- [ ] Gate script executes typecheck, test, and devplan-alignment-gate.
- [ ] Gate script outputs summary.json and report.md with score details.
- [ ] Execution completes successfully confirming positive delta and passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/83-v33-acceptance-gate/83-01-SUMMARY.md`
</output>
