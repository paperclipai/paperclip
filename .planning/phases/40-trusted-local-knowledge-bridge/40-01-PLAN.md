---
phase: 40
plan: 01
status: ready
requirements_addressed:
  - EXT-03
---

# Phase 40 Plan 01 - Trusted Local Knowledge Bridge

<objective>
Make the existing Knowledge Bridge operational through trusted local bridge pairing, sync queue/health evidence, and operator-visible unavailable/stale/conflict state.
</objective>

<truths>
- RT2 DB, wiki pages, graph projection, and audit records remain canonical.
- Local vault markdown is an inspection/edit surface; write-back enters RT2 only through approved import or conflict decisions.
- Bridge pairing and queue state must be company-scoped and deterministic-testable without a live daemon.
- Existing vault export/import provenance and AMBIGUOUS graph edge behavior must not regress.
</truths>

<tasks>

1. Add DB schema and migration for local bridge pairing and sync queue state.
   - Extend `packages/db/src/schema/rt2_v33_knowledge_sync.ts`.
   - Add migration `0100_rt2_trusted_local_knowledge_bridge.sql`.
   - Export new tables from `packages/db/src/schema/index.ts`.

2. Add shared contracts and validators.
   - Extend `packages/shared/src/types/rt2-knowledge.ts`.
   - Extend `packages/shared/src/validators/rt2-knowledge.ts`.
   - Re-export through shared index files.

3. Add server service and route behavior.
   - Extend `server/src/services/rt2-knowledge-projector.ts` with pairing, heartbeat, queue, apply status, and health aggregation.
   - Extend `server/src/routes/rt2-knowledge.ts` with company-scoped local bridge endpoints and activity log evidence.

4. Add operator UI.
   - Extend `ui/src/api/rt2-knowledge.ts` and `ui/src/lib/queryKeys.ts`.
   - Extend `ui/src/pages/rt2/KnowledgePage.tsx` Bridge tab with pairing/health/queue evidence.

5. Add deterministic verification.
   - Extend embedded route coverage in `server/src/__tests__/rt2-knowledge-routes.test.ts`.
   - Extend fallback route-contract coverage in `server/src/__tests__/rt2-v23-route-fallback.test.ts`.
   - Run `pnpm typecheck && pnpm test`.

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test`
</verification>

