---
phase: 82-paperclip-residue-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
must_haves:
  truths:
    - "CLEANUP-01: No product-facing UI text contains 'Paperclip'. It should be replaced with 'RealTycoon2', 'RT2', or 'iSens'."
    - "CLEANUP-02: RT2 internal modules correctly wrap or facade raw Paperclip assets/schemas instead of direct exposure."
    - "CLEANUP-03: `@paperclipai/*` packages in the frontend are abstracted behind RT2 domain hooks/components where applicable."
  artifacts:
    - path: ui/src/components
      provides: Product-facing UI components
      exports: N/A
  key_links: []
---

<objective>
Scan the codebase to clean up the legacy "Paperclip" residue from UI text, verify RT2 contract boundaries, and ensure package imports (`@paperclipai/*`) follow the compatibility layer pattern rather than leaking into the product-facing UI.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-paperclip-residue-cleanup/82-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search and replace 'Paperclip' branding text in UI components</name>
  <files>ui/src/components/**/*.tsx, ui/src/pages/**/*.tsx</files>
  <read_first></read_first>
  <action>
**Verify and Fix CLEANUP-01: UI Text & Branding**

1. Search for literal string "Paperclip" in user-facing texts within `ui/src/components` and `ui/src/pages`.
2. Exclude system identifiers, CSS class names (e.g., `paperclip-mdxeditor`), and localStorage keys unless they are directly rendered to the user.
3. Replace with "RealTycoon2" or "RT2" according to context.
  </action>
  <verify>
    <automated>
      rg "Paperclip" ui/src/components ui/src/pages --type tsx | grep -v "className" | grep -v "import" | grep -v "localStorage"
    </automated>
  </verify>
  <acceptance_criteria>
    - No direct "Paperclip" text is shown to the user in product-facing components.
  </acceptance_criteria>
  <done>UI text branding cleanup complete.</done>
</task>

<task type="auto">
  <name>Task 2: Review and wrap RT2 Schema/API Contracts</name>
  <files>server/src/services/**/*.ts</files>
  <read_first></read_first>
  <action>
**Verify CLEANUP-02: Schema/API Contracts**

1. Identify places where RT2 APIs expose raw Paperclip schemas instead of RT2-controlled contracts.
2. Abstract these usages behind RT2 Facade or contract layers.
  </action>
  <verify>
    <automated>
      pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - RT2 modules use RT2-controlled contracts for external data boundaries.
  </acceptance_criteria>
  <done>RT2 Schema/API Contract wrapping complete.</done>
</task>

<task type="auto">
  <name>Task 3: Abstract @paperclipai/* package imports in UI</name>
  <files>ui/src/components/**/*.tsx</files>
  <read_first></read_first>
  <action>
**Verify CLEANUP-03: Package Usage Abstracting**

1. Review imports from `@paperclipai/*` within `ui/src/components`.
2. Ensure they are isolated to compatibility layers or wrapped in custom RT2 hooks/components where product-facing domain logic is involved.
  </action>
  <verify>
    <automated>
      rg "from \"@paperclipai/" ui/src/components
    </automated>
  </verify>
  <acceptance_criteria>
    - `@paperclipai/*` imports are appropriately abstracted or used as compatibility layer only.
  </acceptance_criteria>
  <done>Package import abstraction verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A for text cleanup

## STRIDE Threat Register
N/A
</threat_model>

<verification>
Run the following in sequence:

```bash
pnpm typecheck
```
</verification>

<success_criteria>
- [ ] No product-facing text contains "Paperclip"
- [ ] Schema boundaries respect RT2 contracts
- [ ] Frontend `@paperclipai/*` usages are isolated
- [ ] pnpm typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/82-paperclip-residue-cleanup/82-01-SUMMARY.md`
</output>
