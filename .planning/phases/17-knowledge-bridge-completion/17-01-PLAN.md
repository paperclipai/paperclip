# Phase 17 Plan: Knowledge Bridge Completion

## Goal

`KNOW-01`을 닫기 위해 Obsidian-compatible export/import preview, wiki projector, graph report, evidence status를 `Knowledge` 운영 흐름에 연결한다.

## Tasks

- [x] Shared knowledge contract 확장
  - vault import preview input/result type 추가
  - evidence status enum 추가
  - validator와 export 동기화

- [x] Server route/service 구현
  - `POST /api/companies/:companyId/rt2/knowledge/vault-import-preview`
  - company access guard 유지
  - vault frontmatter의 page key/type/company/source event evidence 검증

- [x] Operator UI 연결
  - `Knowledge` page에 `Bridge` tab 추가
  - projection 실행 버튼 추가
  - vault export 파일 목록 표시
  - import preview 실행과 evidence status 표시
  - graph report confidence summary 표시

- [x] Verification
  - shared validator test
  - server route test
  - UI/shared/server typecheck

## Acceptance Mapping

- `KNOW-01`: 완료. 운영자는 하나의 `Knowledge > Bridge` 흐름에서 export/import preview, graph report, evidence status를 확인할 수 있다.

## Risks

- Import는 preview다. 실제 Obsidian directory write와 bidirectional sync는 이번 phase의 안전 범위를 넘는다.
- Graph report는 기존 project-scoped graph report를 사용한다. company-wide graph report는 후속 확장이다.
