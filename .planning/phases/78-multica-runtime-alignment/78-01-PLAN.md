---
phase: 78-multica-runtime-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/rt2-task.ts
  - packages/shared/src/validators/rt2-task.ts
  - packages/shared/src/rt2-task.test.ts
  - packages/db/src/schema/rt2_v33_execution_attempts.ts
  - server/src/services/rt2-task-execution.ts
  - server/src/services/rt2-task-engine.ts
  - server/src/routes/rt2-tasks.ts
  - server/src/__tests__/rt2-task-routes.test.ts
  - ui/src/components/Rt2TaskList.tsx
  - ui/src/components/Rt2TaskList.test.tsx
  - ui/src/components/Rt2TaskPanel.tsx
  - ui/src/components/Rt2TaskPanel.test.tsx
autonomous: false
requirements:
  - MULTICA-01
  - MULTICA-02
  - MULTICA-03
user_setup: []
must_haves:
  truths:
    - "MULTICA-01: RT2 execution uses canonical queued -> dispatched -> running -> completed/failed/cancelled lifecycle with transition guards in service and domain events."
    - "MULTICA-02: Runtime capacity, stale cleanup, and progress stream are visible on work cards and Jarvis surfaces."
    - "MULTICA-03: Domain events append to event stream and timeline reads them back correctly."
  artifacts:
    - path: "packages/shared/src/types/rt2-task.ts"
      provides: "Rt2ExecutionState with dispatched/claimed, Rt2ExecutionTimelineEvent interface"
    - path: "server/src/services/rt2-task-execution.ts"
      provides: "normalizeExecutionState() mapping claimed->dispatched, lifecycle transition guards"
    - path: "server/src/services/rt2-task-engine.ts"
      provides: "Timeline building from domain events and heartbeat_run_events"
    - path: "ui/src/components/Rt2TaskPanel.tsx"
      provides: "Execution evidence surface on task detail panel"
  key_links:
    - from: "packages/shared/src/types/rt2-task.ts"
      to: "packages/db/src/schema/rt2_v33_execution_attempts.ts"
      via: "Rt2ExecutionState type used in CHECK constraint"
      pattern: "Rt2ExecutionState"
    - from: "server/src/services/rt2-task-execution.ts"
      to: "packages/db/src/schema/heartbeat_run_events.ts"
      via: "listTimeline reads heartbeat run events for progress/message/tool evidence"
      pattern: "heartbeat_run_events"
    - from: "server/src/services/rt2-jarvis.ts"
      to: "server/src/services/rt2-task-execution.ts"
      via: "Jarvis surfaces consume execution summaries with normalized state"
      pattern: "normalizeExecutionState"
---

<objective>
Verify Phase 78: Multica Runtime Alignment

Prove MULTICA-01, MULTICA-02, and MULTICA-03 by verifying Phase 67's implementation actually works:
- Lifecycle transitions (queued → dispatched → running → completed/failed/cancelled) are enforced in code
- Runtime capacity, stale cleanup, and progress stream are visible on work card/Jarvis surfaces
- Domain events append to event stream and timeline reads them back

This phase does NOT implement new features — it verifies Phase 67's existing implementation.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/78-multica-runtime-alignment/78-CONTEXT.md
@.planning/phases/67-multica-runtime-execution-alignment/67-CONTEXT.md
@.planning/phases/67-multica-runtime-execution-alignment/67-VERIFICATION.md
@packages/shared/src/types/rt2-task.ts
@packages/db/src/schema/rt2_v33_execution_attempts.ts
@server/src/services/rt2-task-execution.ts
@server/src/services/rt2-task-engine.ts
@server/src/services/rt2-jarvis.ts
@ui/src/components/Rt2TaskPanel.tsx
@ui/src/components/Rt2TaskList.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted input crosses here — execution state transitions |
| API → DB | Schema CHECK constraint enforces valid state transitions |
| Service → Event Stream | Domain events must append correctly for timeline reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Integrity | Execution state machine | mitigate | Verify normalizeExecutionState() is called everywhere execution state is read for product surfaces |
| T-78-02 | Integrity | Timeline evidence | mitigate | Verify listTimeline returns domain events + heartbeat_run_events merged correctly |
| T-78-03 | Product trust | UI state display | mitigate | Verify UI surfaces show 'dispatched' not 'claimed' in Korean-first copy |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify MULTICA-01 — Lifecycle transition guards in code</name>
  <files>packages/shared/src/types/rt2-task.ts, packages/db/src/schema/rt2_v33_execution_attempts.ts, server/src/services/rt2-task-execution.ts</files>
  <action>
Verify the following in code:

**1. Rt2ExecutionState type (packages/shared/src/types/rt2-task.ts):**
- [ ] Line 11-19: Rt2ExecutionState includes "dispatched" as canonical post-queue state
- [ ] Line 14: "claimed" is present (for backward compat in DB)

**2. DB CHECK constraint (packages/db/src/schema/rt2_v33_execution_attempts.ts):**
- [ ] Line 48: CHECK constraint allows 'dispatched', 'claimed', 'running', 'completed', 'failed', 'cancelled'
- [ ] This confirms both states are valid in DB

**3. normalizeExecutionState() (server/src/services/rt2-task-execution.ts):**
- [ ] Line 36-38: Function maps 'claimed' → 'dispatched' at read time
- [ ] This confirms product surfaces see 'dispatched' only

**4. Lifecycle method structure (rt2-task-execution.ts):**
- [ ] dispatch() method sets state to 'dispatched' (not 'claimed')
- [ ] start() method transitions 'dispatched' → 'running' (not 'claimed' → 'running')
- [ ] complete/fail/cancel set terminal states correctly

**Run verification:**
```bash
grep -n "dispatched" packages/shared/src/types/rt2-task.ts | head -10
grep -n "normalizeExecutionState" server/src/services/rt2-task-execution.ts
grep -n "state.*dispatched" server/src/services/rt2-task-execution.ts | head -10
```
  </action>
  <verify>
grep -n "normalizeExecutionState" server/src/services/rt2-task-execution.ts
# Should show: function normalizeExecutionState(state: string): Rt2Summary["state"] { return (state === "claimed" ? "dispatched" : state) }
  </verify>
  <done>
normalizeExecutionState() correctly maps claimed→dispatched at line 36-38
Rt2ExecutionState includes "dispatched" (line 13)
DB CHECK constraint (line 48) allows both dispatched and claimed
Product surfaces read through normalizeExecutionState() show "dispatched" only
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MULTICA-02 — Runtime evidence on work cards/Jarvis</name>
  <files>ui/src/components/Rt2TaskPanel.tsx, ui/src/components/Rt2TaskList.tsx, server/src/services/rt2-jarvis.ts</files>
  <action>
Verify the following in code:

**1. Jarvis state mapping (server/src/services/rt2-jarvis.ts):**
- [ ] Line 586: executionState maps 'claimed' → 'dispatched'
- [ ] Line 603: Korean copy shows normalized state (not raw 'claimed')

**2. Work card execution display (ui/src/components/Rt2TaskPanel.tsx):**
- [ ] Shows execution state (should be 'dispatched' after normalization)
- [ ] Shows executor/runtime identity
- [ ] Shows heartbeat/run freshness (latestTimelineEvent)
- [ ] Shows failure/cancellation reason if present

**3. Task list execution display (ui/src/components/Rt2TaskList.tsx):**
- [ ] Compact execution state visible on cards
- [ ] Runtime/executor info visible

**Run verification:**
```bash
grep -n "claimed.*dispatched\|dispatched.*claimed" server/src/services/rt2-jarvis.ts
grep -n "executionState\|latestTimelineEvent" ui/src/components/Rt2TaskPanel.tsx | head -15
```
  </action>
  <verify>
grep -n "claimed.*dispatched" server/src/services/rt2-jarvis.ts
# Should show: line 586: executionState: latestExecution?.state === "claimed" ? "dispatched" : ...
  </verify>
  <done>
Jarvis rt2-jarvis.ts line 586 maps claimed→dispatched correctly
Task panel shows execution state with normalized value
Task list shows compact execution evidence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MULTICA-03 — Event/projector integration</name>
  <files>server/src/services/rt2-task-execution.ts, server/src/services/rt2-task-engine.ts</files>
  <action>
Verify the following in code:

**1. Domain events emitted (rt2-task-execution.ts):**
- [ ] dispatch() emits rt2.execution.dispatched domain event
- [ ] start() emits rt2.execution.started domain event
- [ ] complete/fail/cancel emit appropriate domain events

**2. Timeline building (rt2-task-engine.ts or rt2-task-execution.ts):**
- [ ] listTimeline or equivalent function reads from rt2_domain_events table
- [ ] listTimeline reads from heartbeat_run_events table
- [ ] Merges both sources into Rt2ExecutionTimelineEvent[]

**3. Event projector verification:**
- [ ] Domain events are appended with idempotency keys
- [ ] Timeline reads events back correctly

**Run verification:**
```bash
grep -n "rt2.execution.dispatched\|domainEventService" server/src/services/rt2-task-execution.ts | head -15
grep -n "listTimeline\|heartbeat_run_events" server/src/services/rt2-task-engine.ts | head -15
```
  </action>
  <verify>
grep -n "domainEvents\|heartbeat_run_events" server/src/services/rt2-task-engine.ts | head -20
# Should show domain events and heartbeat events being queried for timeline
  </verify>
  <done>
dispatch() emits rt2.execution.dispatched with idempotency key
listTimeline reads from both rt2_domain_events and heartbeat_run_events
Timeline correctly merges lifecycle events from domain events and progress from heartbeat
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify tests pass — Lifecycle and timeline</name>
  <files>packages/shared/src/rt2-task.test.ts, server/src/__tests__/rt2-task-routes.test.ts</files>
  <action>
Run the existing Phase 67 tests to verify they still pass:

```bash
pnpm exec vitest run packages/shared/src/rt2-task.test.ts
```

If tests fail, investigate and fix. The tests should verify:
- normalizeExecutionState maps claimed→dispatched
- State machine transitions work correctly
- Timeline building works

Report test results.
  </action>
  <verify>
pnpm exec vitest run packages/shared/src/rt2-task.test.ts
  </verify>
  <done>
All shared execution contract tests pass
Type definitions are correct for dispatched/claimed states
  </done>
</task>

</tasks>

<verification>
- [ ] grep confirms normalizeExecutionState() at rt2-task-execution.ts line 36-38
- [ ] grep confirms claimed→dispatched mapping at rt2-jarvis.ts line 586
- [ ] grep confirms domain event emission for dispatched lifecycle edge
- [ ] grep confirms timeline reads from both domain_events and heartbeat_run_events
- [ ] pnpm exec vitest run packages/shared/src/rt2-task.test.ts passes
- [ ] pnpm typecheck passes
</verification>

<success_criteria>
- MULTICA-01: Lifecycle queued→dispatched→running→completed/failed/cancelled with transition guards verified in code
- MULTICA-02: Runtime evidence (state, executor, freshness) visible on work card/Jarvis surfaces verified
- MULTICA-03: Domain events append and timeline reads them back verified
- All Phase 78 verification artifacts created
- No new implementation — Phase 67 implementation verified to work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/78-multica-runtime-alignment/78-01-SUMMARY.md`
</output>