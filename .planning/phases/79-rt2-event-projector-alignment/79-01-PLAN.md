---
phase: 79-rt2-event-projector-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/rt2_v33_domain_events.ts
  - packages/shared/src/types/rt2-domain-events.ts
  - packages/shared/src/validators/rt2-domain-events.ts
  - packages/shared/src/rt2-domain-events.test.ts
  - packages/shared/src/rt2-task.test.ts
  - server/src/services/rt2-domain-events.ts
  - server/src/services/rt2-task-execution.ts
  - server/src/services/rt2-task-engine.ts
autonomous: true
requirements:
  - MULTICA-03
  - RT2-01
  - RT2-02
  - RT2-03
must_haves:
  truths:
    - "MULTICA-03: Events append to `rt2_v33_domain_events` via `appendAndProject` and timeline reads them back via `listTimeline`"
    - "RT2-01: Append-only event stream with idempotency keys prevents duplicate event emission on retry"
    - "RT2-02: Execution lifecycle events (`rt2.execution.dispatched/started/completed/failed/cancelled/stale_cleaned`) are emitted with correct idempotency keys"
    - "RT2-03: Work/Task/Deliverable lifecycle uses RT2-native event contracts (`rt2.task.created`, `rt2.todo.created`, `rt2.deliverable.defined`, `rt2.participant.joined/assigned/ended`, `rt2.task.capacity_changed`, `rt2.todo.started`) — no legacy Paperclip patterns"
  artifacts:
    - path: packages/db/src/schema/rt2_v33_domain_events.ts
      provides: Domain events table with idempotency key unique index (companyIdempotencyUq) and append-only semantics
      exports: rt2V33DomainEvents, rt2V33ProjectorState, rt2V33ProjectorEvents
    - path: server/src/services/rt2-domain-events.ts
      provides: Canonical event append path with idempotency deduplication
      exports: appendAndProject (line 279)
    - path: server/src/services/rt2-task-execution.ts
      provides: Execution lifecycle with all idempotency keys, buildTimelineEvents merging domain+heartbeat events
      exports: dispatch, start, complete, fail, cancel, cleanupStale, listTimeline, appendExecutionEvent
    - path: server/src/services/rt2-task-engine.ts
      provides: Task/todo/participant CRUD with RT2-native domain events
      exports: createTask, createTodo, addParticipant, updateCapacity, endParticipant, startTodo
  key_links:
    - from: server/src/services/rt2-task-execution.ts:appendExecutionEvent
      to: server/src/services/rt2-domain-events.ts:appendAndProject
      via: await domainEvents.appendAndProject(...)
      pattern: "appendExecutionEvent.*appendAndProject"
    - from: server/src/services/rt2-task-execution.ts:listTimeline
      to: packages/db/src/schema/rt2_v33_domain_events.ts
      via: buildTimelineEvents reads rt2_v33_domain_events + heartbeat_run_events, merged and sorted
      pattern: "buildTimelineEvents.*sort.*createdAt.*seq"
    - from: server/src/services/rt2-task-execution.ts:dispatch
      to: packages/db/src/schema/rt2_v33_execution_attempts.ts
      via: updateState sets state=dispatched then appendExecutionEvent emits rt2.execution.dispatched
      pattern: "idempotencyKey.*rt2\\.execution\\.dispatched"
    - from: server/src/services/rt2-task-engine.ts:createTask
      to: packages/db/src/schema/rt2_v33_domain_events.ts
      via: txEvents.appendAndProject emits rt2.task.created with idempotency key rt2.task.created:${issue.id}
      pattern: "rt2\\.task\\.created.*rt2\\.deliverable\\.defined"
---

<objective>
Verify RT2 event/projector alignment for MULTICA-03, RT2-01, RT2-02, RT2-03. This phase does NOT implement new features — it verifies that Phase 67's design is correctly implemented in actual code. All verification uses grep + typecheck + test assertions against existing code.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-rt2-event-projector-alignment/79-CONTEXT.md
@packages/db/src/schema/rt2_v33_domain_events.ts
@server/src/services/rt2-domain-events.ts
@server/src/services/rt2-task-execution.ts
@server/src/services/rt2-task-engine.ts
@packages/shared/src/types/rt2-domain-events.ts
@packages/shared/src/validators/rt2-domain-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MULTICA-03 — Event/Projector Integration</name>
  <files>packages/db/src/schema/rt2_v33_domain_events.ts, server/src/services/rt2-domain-events.ts, server/src/services/rt2-task-execution.ts</files>
  <action>
**Verify `appendAndProject` is the single write path for RT2 domain events.**
1. Grep `packages/db/src/schema/rt2_v33_domain_events.ts` to confirm:
   - `companyIdempotencyUq` unique index on `(companyId, idempotencyKey)` WHERE idempotencyKey IS NOT NULL (line 44-46)
   - No UPDATE or DELETE operations on `rt2_v33_domain_events` — table is append-only
2. Grep `server/src/services/rt2-domain-events.ts` to confirm:
   - `appendAndProject` at line 279 calls `append()` then `processEvent("rt2.activity_live_bridge", ...)` then `rt2KnowledgeProjectorService(db).projectEvent(...)`
   - `append()` at line 93 checks idempotency key uniqueness before insert (line 96-110) — if existing record found, returns it without inserting (replay-safe)
3. Grep `server/src/services/rt2-task-execution.ts` to confirm `listTimeline` at line 652-655 calls `buildTimelineEvents` which:
   - Reads from `rt2_v33_domain_events` (line 232-238) ordered by `occurredAt, createdAt`
   - Reads from `heartbeat_run_events` (line 242-248) ordered by `seq, createdAt` (when heartbeatRunId exists)
   - Merges both sorted arrays by `createdAt ASC, seq ASC` (line 254-256)
   - Returns `Rt2ExecutionTimelineEvent[]`

**Verify MULTICA-03 truth:** Events append via `appendAndProject` → `rt2_v33_domain_events`, timeline reads them back correctly via `listTimeline` + `buildTimelineEvents`.
  </action>
  <verify>
    <automated>
      grep -c "companyIdempotencyUq" packages/db/src/schema/rt2_v33_domain_events.ts &&
      grep -c "appendAndProject" server/src/services/rt2-domain-events.ts &&
      grep -c "buildTimelineEvents" server/src/services/rt2-task-execution.ts &&
      grep -c "listTimeline" server/src/services/rt2-task-execution.ts
    </automated>
  </verify>
  <done>appendAndProject is the single event write path confirmed; listTimeline reads from rt2_v33_domain_events AND heartbeat_run_events with correct merge sort</done>
</task>

<task type="auto">
  <name>Task 2: Verify RT2-01 + RT2-02 — Idempotency Keys and Execution Lifecycle Events</name>
  <files>server/src/services/rt2-task-execution.ts, packages/shared/src/types/rt2-domain-events.ts, packages/shared/src/validators/rt2-domain-events.ts</files>
  <action>
**Verify RT2-01: Append-only event stream with idempotency keys.**
Grep `server/src/services/rt2-task-execution.ts` to confirm all 8 execution lifecycle events use idempotency keys:
- `rt2.execution.enqueued` → idempotency key `rt2.execution.enqueued:${attempt.id}` (line 450)
- `rt2.execution.dispatched` → `rt2.execution.dispatched:${attemptId}` (line 395)
- `rt2.execution.started` → `rt2.execution.started:${attemptId}` (line 499)
- `rt2.execution.completed` → `rt2.execution.completed:${attemptId}` (line 538)
- `rt2.execution.failed` → `rt2.execution.failed:${attemptId}` (line 564)
- `rt2.execution.cancelled` → `rt2.execution.cancelled:${attemptId}` (line 590)
- `rt2.execution.stale_cleaned` → `rt2.execution.stale_cleaned:${candidate.id}:${staleBefore.toISOString()}` (line 636)
- `rt2.execution.retried` → `rt2.execution.retried:${attempt.id}:${retryAttempt.id}` (line 682)

**Verify RT2-02: Execution lifecycle integration.**
Confirm `appendExecutionEvent` (line 337-362) is called after every state transition in dispatch/start/complete/fail/cancel/cleanupStale/retry. Grep confirms:
- `dispatch()` (line 390-403): updateState → appendExecutionEvent for `rt2.execution.dispatched`
- `start()` (line 494-504): updateState → appendExecutionEvent for `rt2.execution.started`
- `complete()` (line 533-543): updateState → appendExecutionEvent for `rt2.execution.completed`
- `fail()` (line 559-568): updateState → appendExecutionEvent for `rt2.execution.failed`
- `cancel()` (line 585-594): updateState → appendExecutionEvent for `rt2.execution.cancelled`
- `cleanupStale()` (line 631-642): updateState → appendExecutionEvent for `rt2.execution.stale_cleaned`
- `retry()` (line 677-686): insert new attempt → appendExecutionEvent for `rt2.execution.retried`

**Verify RT2-02 runtime integration decisions (D-05 to D-09 from CONTEXT):**
- `assertRuntimeCanAccept` (line 164-199) checks runtime status/health/freshness/capacity before dispatch
- `dispatch()` (line 364-404) sets state=dispatched (not claimed), atomic runtime service update + domain event
- `startableStates = ["dispatched", "claimed"]` (line 34) — claimed state accepted for backward compat
- `runtimeActiveStates = ["dispatched", "claimed", "running"]` (line 33) — used by fail/cancel/cleanupStale
- `cancel()` (line 572-595) transitions ["queued", ...runtimeActiveStates] → cancelled
- `cleanupStale()` (line 598-649) transitions stale dispatched/claimed/running → failed with "stale_runtime_cleanup"
  </action>
  <verify>
    <automated>
      grep -E "idempotencyKey.*rt2\\.execution\\.(enqueued|dispatched|started|completed|failed|cancelled|stale_cleaned|retried)" server/src/services/rt2-task-execution.ts | wc -l &&
      grep -c "appendExecutionEvent" server/src/services/rt2-task-execution.ts &&
      grep -c "assertRuntimeCanAccept" server/src/services/rt2-task-execution.ts
    </automated>
  </verify>
  <done>All 8 execution lifecycle events have idempotency keys; every state transition in dispatch/start/complete/fail/cancel/cleanupStale/retry calls appendExecutionEvent</done>
</task>

<task type="auto">
  <name>Task 3: Verify RT2-03 — RT2-Native Operation Contracts</name>
  <files>server/src/services/rt2-task-engine.ts, packages/shared/src/types/rt2-domain-events.ts, packages/shared/src/validators/rt2-domain-events.ts</files>
  <action>
**Verify RT2-03: Work/Task/Deliverable lifecycle uses RT2-native event contracts.**
Grep `server/src/services/rt2-task-engine.ts` for all RT2-native domain event emissions:
1. `rt2.task.created` (line 648) — in `createTask` tx, idempotency key `rt2.task.created:${issue.id}`
2. `rt2.deliverable.defined` (line 668, 793) — in `createTask` and `createTodo` tx, idempotency key `rt2.deliverable.defined:${deliverable.id}`
3. `rt2.todo.created` (line 775) — in `createTodo` tx, idempotency key `rt2.todo.created:${todo.id}`
4. `rt2.todo.started` (line 953) — in `startTodo`, idempotency key `rt2.todo.started:${todo.id}`
5. `rt2.participant.joined` / `rt2.participant.assigned` (line 404) — in `addParticipant`, idempotency key `rt2.participant.${participantMutation}:${participant.id}`
6. `rt2.participant.ended` (line 900) — in `endParticipant` tx, idempotency key `rt2.participant.ended:${taskIssueId}:${userId}:${Date.now()}`
7. `rt2.task.capacity_changed` (line 859) — in `updateCapacity` tx, idempotency key `rt2.task.capacity_changed:${taskIssueId}:${Date.now()}`

**Verify NO legacy Paperclip patterns in RT2 surfaces:**
Grep `packages/shared/src/types/rt2-domain-events.ts` and `packages/shared/src/validators/rt2-domain-events.ts` to confirm:
- Valid event types include only RT2-native contracts (rt2.task.*, rt2.todo.*, rt2.deliverable.*, rt2.participant.*, rt2.execution.*)
- No `WorkQueue`, `AgentTask`, `createIssue` legacy patterns in RT2 service names

Grep `server/src/services/rt2-task-engine.ts` and `rt2-task-execution.ts` to confirm:
- Service names are `rt2-task-engine` and `rt2-task-execution` (rt2-* namespaced, no legacy Paperclip naming)
- `normalizeExecutionState` at line 206-208 (rt2-task-engine.ts) and line 36-38 (rt2-task-execution.ts) both map `claimed` → `dispatched` for UI surfaces

**Verify `rt2_v33_domain_events` schema is append-only:**
Grep `packages/db/src/schema/rt2_v33_domain_events.ts` confirms only INSERT operations (no UPDATE/DELETE on event records).
  </action>
  <verify>
    <automated>
      grep -E "rt2\\.(task|todo|deliverable|participant)\\.(created|defined|joined|assigned|ended|started|capacity_changed)" server/src/services/rt2-task-engine.ts | wc -l &&
      grep -c "rt2-task-engine" server/src/services/rt2-task-engine.ts &&
      grep -c "rt2-task-execution" server/src/services/rt2-task-execution.ts &&
      grep -c "normalizeExecutionState" server/src/services/rt2-task-engine.ts &&
      grep -c "normalizeExecutionState" server/src/services/rt2-task-execution.ts
    </automated>
  </verify>
  <done>All 7 RT2-native operation contracts confirmed in rt2-task-engine.ts; service names rt2-* namespaced; normalizeExecutionState in both services maps claimed→dispatched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime → Database | Runtime service writes state changes; domain events must be atomic |
| API → appendAndProject | Untrusted input crosses at AppendRt2DomainEvent schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering | rt2-task-execution.ts | mitigate | Idempotency key unique index on (companyId, idempotencyKey) prevents duplicate events; append() checks existing before insert (line 96-110 of rt2-domain-events.ts) |
| T-79-02 | Denial | listTimeline/buildTimelineEvents | accept | Timeline ordering uses createdAt ASC, seq ASC tiebreaker — deterministic, monotonic; race between concurrent timeline reads is read-only |
| T-79-03 | Information | rt2-task-engine.ts | mitigate | RT2-native event contracts use typed Rt2DomainEventType union; appendRt2DomainEventSchema validates eventType against allowed list |
</threat_model>

<verification>
Run the following in sequence:

```bash
# 1. TypeScript type check
pnpm typecheck

# 2. Unit tests for RT2 task and domain events
pnpm exec vitest run packages/shared/src/rt2-task.test.ts
pnpm exec vitest run packages/shared/src/rt2-domain-events.test.ts
pnpm exec vitest run server/src/__tests__/rt2-domain-events.test.ts
```
</verification>

<success_criteria>
- [ ] grep confirms `appendAndProject` is called for all 8 execution lifecycle events (enqueued/dispatched/started/completed/failed/cancelled/stale_cleaned/retried)
- [ ] grep confirms all 8 execution lifecycle events have idempotency keys with attemptId included
- [ ] grep confirms `listTimeline` reads from BOTH `rt2_v33_domain_events` AND `heartbeat_run_events`
- [ ] grep confirms `companyIdempotencyUq` unique index on (companyId, idempotencyKey) in rt2_v33_domain_events.ts
- [ ] grep confirms all 7 RT2-native event types in rt2-task-engine.ts: rt2.task.created, rt2.todo.created, rt2.deliverable.defined (×2), rt2.participant.joined/assigned, rt2.participant.ended, rt2.task.capacity_changed, rt2.todo.started
- [ ] grep confirms service names are `rt2-task-engine` and `rt2-task-execution` (rt2-namespaced, no legacy Paperclip patterns)
- [ ] pnpm typecheck passes
- [ ] pnpm exec vitest run packages/shared/src/rt2-task.test.ts passes
</success_criteria>

<output>
After completion, create `.planning/phases/79-rt2-event-projector-alignment/79-01-SUMMARY.md`
</output>
