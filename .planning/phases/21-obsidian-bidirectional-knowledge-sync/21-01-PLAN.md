---
phase: 21
plan: 01
title: Obsidian Bidirectional Knowledge Sync
status: complete
requirements_addressed: [KNOW-02, KNOW-03, KNOW-04]
wave: 1
---

# Plan 21-01: Obsidian Bidirectional Knowledge Sync

<objective>
Knowledge Bridge를 preview-only 상태에서 운영자가 저장, 검수, 승인, 충돌 해결할 수 있는 bidirectional sync flow로 고도화한다.
</objective>

<tasks>

1. Shared contracts
   - Add vault writer settings, dry-run result, import candidate, import apply, and conflict resolution types.
   - Add validators for writer save, import apply, and conflict resolution.

2. RT2-controlled persistence
   - Add `rt2_v33_knowledge_vault_settings`.
   - Add `rt2_v33_knowledge_sync_decisions`.
   - Add migration `0075_rt2_phase21_knowledge_sync.sql`.

3. Backend service/routes
   - Save vault writer settings and dry-run export target/conflict risk.
   - Expand import preview into wiki page, graph node, and graph edge candidates.
   - Apply only approved candidates.
   - Resolve conflicts with `rt2_wins`, `vault_wins`, or `manual_merge`.
   - Log important knowledge sync mutations.

4. Frontend Bridge UI
   - Add local vault writer settings and dry-run result.
   - Show import candidates with approval checkboxes.
   - Add apply-approved and conflict-resolution controls.

5. Verification and tracking
   - Extend fallback route contract test.
   - Run typechecks, migration numbering check, and focused route test.
   - Update GSD milestone documents.

</tasks>

<verification>

- `pnpm --filter @paperclipai/shared typecheck`
- `pnpm --filter @paperclipai/db typecheck`
- `pnpm --filter @paperclipai/server typecheck`
- `pnpm --filter @paperclipai/ui typecheck`
- `pnpm --filter @paperclipai/db run check:migrations`
- `pnpm exec vitest run server/src/__tests__/rt2-v23-route-fallback.test.ts`

</verification>
