---
phase: 32
phase_name: Lint Traceability and Milestone Acceptance Closure
plan: 1
type: implementation
wave: 1
depends_on: []
autonomous: true
must_haves:
  truths:
    - Phase 29 lint acceptance is based on code, test, verification, validation, and summary-frontmatter evidence, not roadmap or plan text alone.
    - LINT-01 through LINT-04 are accepted only if focused lint verification still supports the existing Phase 29 claims.
    - Phase 29 has Nyquist validation scenarios for scheduled execution, evidence-only findings, embedding_consistency, read-only behavior, and no on-write trigger.
    - Phase 32 records final v2.4 milestone acceptance without erasing the original audit gap context.
    - No source code is changed unless validation exposes a concrete lint implementation gap.
  artifacts:
    - .planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md
    - .planning/phases/29-consistency-linting-batch/29-VALIDATION.md
    - .planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-01-SUMMARY.md
    - .planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md
    - .planning/v2.4-MILESTONE-REAUDIT.md
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md
      via: requirements-completed frontmatter for LINT-01..LINT-04
    - from: .planning/phases/29-consistency-linting-batch/29-VERIFICATION.md
      to: .planning/phases/29-consistency-linting-batch/29-VALIDATION.md
      via: evidence-backed Nyquist scenarios for each lint behavior
    - from: .planning/phases/30-knowledge-artifact-and-verification-closure/30-VERIFICATION.md
      to: .planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md
      via: graph/wiki stabilization evidence feeding scheduled lint acceptance
    - from: .planning/phases/31-economy-artifact-and-verification-closure/31-VERIFICATION.md
      to: .planning/v2.4-MILESTONE-REAUDIT.md
      via: economy closure prerequisite for final milestone acceptance
requirements:
  - LINT-01
  - LINT-02
  - LINT-03
  - LINT-04
files_modified:
  - .planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md
  - .planning/phases/29-consistency-linting-batch/29-VALIDATION.md
  - .planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-01-SUMMARY.md
  - .planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md
  - .planning/v2.4-MILESTONE-REAUDIT.md
---

# Plan 32-01: Lint Traceability and Milestone Acceptance Closure

<objective>
Close the remaining v2.4 consistency lint audit gaps by repairing Phase 29 requirement traceability, creating Phase 29 Nyquist validation, rerunning focused lint verification, and writing Phase 32 final milestone re-audit artifacts.

This plan is artifact and audit closure first. It does not add lint capabilities unless the focused validation exposes a concrete implementation gap.
</objective>

<truths>
- LINT-01 through LINT-04 must trace through `.planning/REQUIREMENTS.md`, `.planning/phases/29-consistency-linting-batch/29-VERIFICATION.md`, `.planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md`, and `.planning/phases/29-consistency-linting-batch/29-VALIDATION.md`.
- Phase 29 summary frontmatter may list `requirements-completed` for LINT-01 through LINT-04 only if existing verification and focused checks still support acceptance.
- `29-VALIDATION.md` must include Nyquist scenarios for scheduled execution, evidence-only findings, `embedding_consistency`, read-only behavior, and no on-write lint trigger.
- Final v2.4 re-audit must preserve the original audit as historical context and record the post-closure result separately.
- `pnpm test:e2e` is not part of this default closure gate.
</truths>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Phase 29 wiki lint behavior | mitigate | Preserve evidence-only lint behavior. Validate LINT-02 with tests that compare wiki rows before and after linting; do not add auto-fix behavior. |
| T-32-02 | Information Disclosure | Lint route and scoped corpus evidence | mitigate | Acceptance evidence must preserve company/project scoped lint behavior and existing route authorization references; do not weaken `assertCompanyAccess` or scoped queries. |
| T-32-03 | Denial of Service | Scheduled lint runner | mitigate | Validate LINT-04 against nightly window gating and overlap prevention in `createRt2WikiLintScheduler()`; do not add on-write lint execution. |
| T-32-04 | Repudiation | Milestone acceptance artifacts | mitigate | Record exact command outcomes and source/test evidence for each accepted requirement so audit status is reproducible. |
| T-32-05 | Elevation of Privilege | Artifact closure process | accept | This plan changes planning artifacts only by default; no auth/session behavior is modified. If source changes become necessary, preserve existing access-control tests and rerun focused server checks. |
</threat_model>

<tasks>

<task type="auto">
<name>Repair Phase 29 Summary Traceability</name>
<files>
- `.planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md`
</files>
<action>
Inspect `.planning/phases/29-consistency-linting-batch/29-VERIFICATION.md`, `server/src/services/rt2-wiki-lint.ts`, `server/src/app.ts`, `server/src/routes/rt2-daily-report.ts`, and `server/src/__tests__/rt2-wiki-lint.test.ts`. If that evidence still supports LINT-01 through LINT-04, add YAML frontmatter to `29-01-SUMMARY.md` with `phase`, `phase_name`, `plan`, `status`, `completed`, and `requirements-completed` listing only LINT-01, LINT-02, LINT-03, and LINT-04 per D-01.

Do not mark a requirement complete from roadmap, context, or plan text alone per D-04 and D-05. If any LINT requirement is partial, leave it out of `requirements-completed`, record the explicit gap in the summary notes per D-06, and do not inflate milestone completion. Preserve the existing summary content and add traceability metadata rather than rewriting the whole file. This task implements D-01, D-04, D-05, D-06, D-07, D-08, D-09, and D-10.
</action>
<verify>
<automated>$p = '.planning/phases/29-consistency-linting-batch/29-01-SUMMARY.md'; $c = Get-Content -Raw -LiteralPath $p; $frontmatter = [regex]::Match($c, '(?s)^---(.*?)---').Groups[1].Value; foreach ($id in @('LINT-01','LINT-02','LINT-03','LINT-04')) { if ($frontmatter -notmatch [regex]::Escape($id)) { throw "Missing $id in summary frontmatter" } }; $ids = @([regex]::Matches($frontmatter, '\b[A-Z]+-\d+\b') | ForEach-Object { $_.Value }); $bad = @($ids | Where-Object { $_ -notin @('LINT-01','LINT-02','LINT-03','LINT-04') }); if ($bad.Count -gt 0) { throw "Unexpected requirement IDs in summary frontmatter: $($bad -join ', ')" }</automated>
</verify>
<done>
Phase 29 summary frontmatter lists only evidence-supported LINT requirements, with no unrelated requirement IDs and no broad summary rewrite.
</done>
</task>

<task type="auto">
<name>Create Phase 29 Nyquist Validation</name>
<files>
- `.planning/phases/29-consistency-linting-batch/29-VALIDATION.md`
</files>
<action>
Create `29-VALIDATION.md` with frontmatter and Nyquist-style scenario tables that map directly to LINT-01 through LINT-04 per D-02. Include scenarios for scoped pairwise wiki comparison, evidence-only findings, `embedding_consistency` issue production and summary counts, read-only row behavior, scheduler nightly execution, overlap prevention, and absence of on-write lint invocation from daily wiki write/materialization paths.

Each scenario must cite concrete evidence: `rt2WikiLintService.lintWikiPages()` and company/project/date filtering for D-07; evidence fields and row equality tests for D-08; `embedding_consistency`, semantic comparison counts, and contradiction evidence for D-09; `createRt2WikiLintScheduler()`, `server/src/app.ts`, nightly window gating, overlap prevention, and daily report route separation for D-10. Connect Phase 30's WIKI/GRAPH closure to lint acceptance by showing board/domain events produce stable daily wiki pages, graph projection depends on those pages, and scheduled lint reads that stabilized corpus per D-11. Keep the validation scoped to LINT-01 through LINT-04.
</action>
<verify>
<automated>$p = '.planning/phases/29-consistency-linting-batch/29-VALIDATION.md'; if (!(Test-Path -LiteralPath $p)) { throw "Missing $p" }; $c = Get-Content -Raw -LiteralPath $p; foreach ($token in @('LINT-01','LINT-02','LINT-03','LINT-04','embedding_consistency','read-only','on-write','createRt2WikiLintScheduler','rt2WikiLintService.lintWikiPages','30-VERIFICATION.md')) { if ($c -notmatch [regex]::Escape($token)) { throw "Missing $token" } }</automated>
</verify>
<done>
`29-VALIDATION.md` exists and proves the critical lint failure modes at the Nyquist level with code, test, and upstream graph/wiki evidence.
</done>
</task>

<task type="auto">
<name>Run Focused Verification and Close Phase 32</name>
<files>
- `.planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-01-SUMMARY.md`
- `.planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md`
- `.planning/v2.4-MILESTONE-REAUDIT.md`
</files>
<action>
Run focused lint verification first per D-14: `pnpm --filter @paperclipai/server test -- rt2-wiki-lint`, then `pnpm --filter @paperclipai/server typecheck`, then retry `pnpm typecheck` and `pnpm test` if practical. Record exact command outcomes per D-15. Do not run `pnpm test:e2e` per D-16 and AGENTS.md. Do not touch `pnpm-lock.yaml`.

Create Phase 32 summary and verification artifacts showing how LINT-01 through LINT-04 are accepted or explicitly blocked. The verification matrix must cite implementing service file, startup/scheduler wiring, route/API surface, focused test coverage, Phase 29 verification, Phase 29 validation, and command results per D-04. Treat Phase 31 economy closure as a final acceptance prerequisite but do not rework economy artifacts unless the final re-audit finds a traceability blocker per D-12.

Create `.planning/v2.4-MILESTONE-REAUDIT.md` as the post-closure audit result instead of overwriting the original `.planning/v2.4-MILESTONE-AUDIT.md`. Preserve the original audit as historical gap context and identify whether v2.4 now passes requirements, phase artifacts, integration, and flow gates. Any residual issue must be labeled as explicit tech debt with evidence per D-13. Source changes are allowed only if validation exposes a concrete lint implementation gap per D-03; if that happens, restrict changes to the lint service, scheduler wiring, route/result typing, daily wiki integration evidence, or focused lint tests, then rerun the focused lint test and server typecheck.
</action>
<verify>
<automated>pnpm --filter @paperclipai/server test -- rt2-wiki-lint</automated>
<automated>pnpm --filter @paperclipai/server typecheck</automated>
<automated>$files = @('.planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-01-SUMMARY.md','.planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md','.planning/v2.4-MILESTONE-REAUDIT.md'); foreach ($f in $files) { if (!(Test-Path -LiteralPath $f)) { throw "Missing $f" } }; $verification = Get-Content -Raw -LiteralPath '.planning/phases/32-lint-traceability-and-milestone-acceptance-closure/32-VERIFICATION.md'; $reaudit = Get-Content -Raw -LiteralPath '.planning/v2.4-MILESTONE-REAUDIT.md'; foreach ($token in @('LINT-01','LINT-02','LINT-03','LINT-04','WIKI','GRAPH','LEDGER','SETTLE','original audit')) { if ($reaudit -notmatch [regex]::Escape($token)) { throw "Missing $token in re-audit" } }; foreach ($cmd in @('pnpm --filter @paperclipai/server test -- rt2-wiki-lint','pnpm --filter @paperclipai/server typecheck')) { if ($verification -notmatch [regex]::Escape($cmd)) { throw "Missing command outcome for $cmd" } }; foreach ($token in @('exit 0','Exit 0','passed','PASSED')) { if ($verification -match [regex]::Escape($token)) { $hasOutcome = $true } }; if (!$hasOutcome) { throw 'Missing explicit command result status in Phase 32 verification' }</automated>
</verify>
<done>
Focused lint verification and server typecheck outcomes are recorded, Phase 32 artifacts exist, and the v2.4 re-audit result can trace LINT acceptance plus Phase 30 and Phase 31 prerequisite closure.
</done>
</task>

</tasks>

<verification>
Run the following in order:

1. `pnpm --filter @paperclipai/server test -- rt2-wiki-lint`
2. `pnpm --filter @paperclipai/server typecheck`
3. `pnpm typecheck` if practical; record timeout or failure separately from LINT acceptance if focused checks pass
4. `pnpm test` if practical; record unrelated `worktree.test.ts` timeout or host-specific embedded Postgres skips separately from LINT acceptance

Artifact checks:

- `29-01-SUMMARY.md` has `requirements-completed` for LINT-01 through LINT-04 only when evidence supports acceptance.
- `29-VALIDATION.md` contains Nyquist scenarios for LINT-01 through LINT-04 and references the graph/wiki upstream closure.
- `32-VERIFICATION.md` records exact command outcomes and per-requirement evidence.
- `.planning/v2.4-MILESTONE-REAUDIT.md` preserves the original audit context and records post-closure milestone status.
</verification>

<success_criteria>
- Phase 29 summary frontmatter closes the original LINT traceability gap without claiming unsupported requirements.
- Phase 29 has a Nyquist-compliant validation artifact for scheduled, evidence-only, read-only batch lint behavior.
- Phase 32 verification links Phase 30 graph/wiki closure, Phase 31 economy closure, and Phase 29 lint acceptance into a final v2.4 milestone acceptance result.
- No Playwright E2E run is required, no `pnpm-lock.yaml` churn is introduced, and no broad source rewrites are performed.
</success_criteria>
