# Phase 36 Plan - Jarvis Grounded Answers

## Scope

Ground the existing Jarvis task advice response in semantic retrieval evidence and contradiction state.

## Tasks

1. Add grounded citation and warning types for Jarvis advice.
2. Retrieve semantic context with `rt2HybridSearchService` using task title/description and project scope.
3. List open contradiction candidates for the same project and expose them as answer warnings.
4. Include citation link targets for tasks, work artifacts, wiki pages, graph nodes/edges, and contradiction items.
5. Update tests to assert semantic citations, stale warnings, contradiction warnings, and company boundary behavior.
6. Run `pnpm typecheck` and `pnpm test`.

## Acceptance

- Jarvis task advice includes `grounding.citations`.
- Stale retrieved evidence adds a warning.
- Open contradiction candidates add warnings and linkable contradiction citations.
- Existing permission boundary stays route-level via `assertCompanyAccess` plus service-level company filtering.

