---
context: phase
phase: 27-coin-ledger-atomicity
task: verification
total_tasks: unknown
status: in_progress
last_updated: 2026-04-27T19:47:17.3102618+09:00
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Windows process probing can hang - CIM/tasklist and broad process inspection stalled during cleanup - use exact PID checks or .NET Process.GetProcessById only.
- [ ] CONSTRAINT: Embedded Postgres support probing can hang on this Windows host - tests now skip embedded Postgres by default unless PAPERCLIP_ENABLE_EMBEDDED_POSTGRES_TESTS=true.
- [ ] CONSTRAINT: workspace-runtime.test has an unresolved hang in the provisioning/realizeExecutionWorkspace area - isolate one test at a time before running full server or pnpm test.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Broad Windows process cleanup | Earlier cleanup attempts around node/CIM/tasklist caused the session to hang or crash. | blocking | Never kill all node.exe. Only act on explicit PIDs from the current test run, and prefer exact PID APIs. |
| Full-suite retry before isolating hang | `pnpm test` and `vitest run server` timed out silently because a narrower server test still hangs. | blocking | Run `workspace-runtime.test.ts` with `-t` filters until the hanging test is identified. |
| Embedded Postgres support probe on Windows | `getEmbeddedPostgresTestSupport()` hung for more than 45 seconds before test collection. | advisory | Keep Windows default skip in `packages/db/src/test-embedded-postgres.ts`; opt in only with `PAPERCLIP_ENABLE_EMBEDDED_POSTGRES_TESTS=true`. |

<current_state>
The repo is paused during Phase 27 Coin Ledger Atomicity verification, but the working tree contains broader v2.4/RT2 changes from phases 25-27 and related runtime/test fixes.

The immediate state is recovery from an interrupted test/process cleanup session. The last active work was narrowing a hang in `server/src/__tests__/workspace-runtime.test.ts`. The user aborted the command:

```sh
pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "runs a configured provision" --reporter=verbose
```

Do not assume that full `pnpm test` is safe to rerun yet.
</current_state>

<completed_work>

Completed this recovery session:
- Confirmed the specific node PID list from the interrupted cleanup was no longer alive. Did not terminate all node processes.
- Diagnosed old log errors as stale for `rt2-tasks.ts`; current `server` and `shared` TypeScript project checks passed.
- Found that `getEmbeddedPostgresTestSupport()` hangs on Windows and patched `packages/db/src/test-embedded-postgres.ts` to skip embedded Postgres tests by default on Windows.
- Verified the embedded Postgres skip path returns immediately.
- Re-ran RT2 DB/server/shared affected tests; embedded Postgres tests now skip instead of hanging on Windows.
- Fixed `packages/shared/src/rt2-graph.test.ts` to match the expanded RT2 graph node/edge type contract.
- Verified shared/adapters non-DB test group passes.

Verification that passed:
- `pnpm exec tsc --noEmit --pretty false -p server/tsconfig.json`
- `pnpm exec tsc --noEmit --pretty false -p packages/shared/tsconfig.json`
- `pnpm exec vitest run packages/shared/src/rt2-task.test.ts --reporter=verbose`
- `pnpm exec vitest run server/src/__tests__/rt2-task-routes.test.ts server/src/__tests__/rt2-daily-report-routes.test.ts --reporter=verbose` (all skipped on Windows due embedded Postgres)
- `pnpm exec vitest run packages/shared/src/rt2-task.test.ts packages/shared/src/rt2-domain-events.test.ts packages/shared/src/rt2-knowledge.test.ts server/src/__tests__/rt2-task-routes.test.ts server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-domain-events.test.ts server/src/__tests__/rt2-knowledge-projector.test.ts server/src/__tests__/rt2-knowledge-routes.test.ts packages/db/src/client.test.ts packages/db/src/rt2-daily-report-persistence.test.ts --reporter=dot`
- `pnpm typecheck`
- `pnpm exec vitest run packages/shared packages/adapter-utils packages/adapters/codex-local packages/adapters/opencode-local --reporter=dot`
- `pnpm exec vitest run server/src/__tests__/paperclip-skill-utils.test.ts --reporter=verbose`
- `pnpm exec vitest run server/src/__tests__/codex-local-execute.test.ts --reporter=verbose`
- `pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "sanitizeRuntimeServiceBaseEnv|ensureServerWorkspaceLinksCurrent|buildWorkspaceRuntimeDesiredStatePatch|resolveWorkspaceRuntimeReadinessTimeoutSec|resolveShell|normalizeAdapterManagedRuntimeServices" --reporter=dot`
- `pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "creates and reuses a git worktree" --reporter=verbose`
- `pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "rejects reusing|reuses the current|rejects reusing a linked|reuses an already|slugifies|preserves intentional" --reporter=dot`
</completed_work>

<remaining_work>

- Identify the specific hang inside `server/src/__tests__/workspace-runtime.test.ts`, likely in the provisioning tests around lines 688-1009.
- Resume with single-test filters. Start with the command that was aborted:

```sh
pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "runs a configured provision" --reporter=verbose
```

- After that, test these one by one:
  - `uses the latest repo-managed provision script when reusing an existing worktree`
  - `writes an isolated repo-local Paperclip config and worktree branding when provisioning`
  - both `provisions worktree-local pnpm node_modules instead of reusing base-repo links` cases
  - `provisions successfully when install is needed but there are no symlinked node_modules to move`
  - `fails instead of writing an unseeded fallback config when worktree init errors after CLI detection succeeds`
  - `retries worktree-local pnpm install without a frozen lockfile when the lockfile is outdated`
- Only after the hang is fixed, rerun a broader server test group, then `pnpm test`.
</remaining_work>

<decisions_made>

- Embedded Postgres tests now skip by default on Windows. Rationale: the support probe hung before test collection on this host, so local Windows verification must avoid blocking by default. Linux/CI behavior remains available unless the env says otherwise.
- RT2 graph shared contract test was updated to match already-expanded constants/types for `deliverable`, `actor`, `event`, and the new graph edge types.
- Cleanup must remain PID-specific. Rationale: user explicitly objected to broad node cleanup and the environment has already shown Windows process inspection instability.
</decisions_made>

<blockers>

- `workspace-runtime.test.ts` full and broad-filter runs hang:
  - `pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "realizeExecutionWorkspace" --reporter=verbose` timed out after 124 seconds.
  - `pnpm exec vitest run server --reporter=dot` timed out after 184 seconds.
  - `pnpm test` timed out after 244 seconds with no output.
- The last single-test run for `"runs a configured provision"` was aborted by the user after about 16 seconds, so its result is unknown.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` - machine-readable summary of current state.
1. `.planning/STATE.md` - milestone/phase state says Phase 27 is current context.
1. `.planning/phases/27-coin-ledger-atomicity/27-01-PLAN.md` - ledger atomicity plan context.
1. `.planning/phases/27-coin-ledger-atomicity/27-02-PLAN.md` - transaction/reconciliation plan context.
1. `packages/db/src/test-embedded-postgres.ts` - Windows embedded Postgres skip change made during recovery.
1. `packages/shared/src/rt2-graph.test.ts` - graph contract test updated during recovery.
1. `server/src/__tests__/workspace-runtime.test.ts` - current unresolved verification hang.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run `pnpm test` immediately. It already timed out silently. Isolate `workspace-runtime.test.ts` first.
- Do not use broad Windows process enumeration/cleanup. Avoid CIM/tasklist and do not kill all node processes.
- Do not revert unrelated dirty working tree files. There is a large in-progress RT2/v2.4 change set.
- Do not treat embedded Postgres skipped tests as Linux/CI coverage. On Windows they are intentionally skipped; CI or explicit opt-in still needs real DB coverage.

## Infrastructure State

- No dev server intentionally left running by this handoff.
- The previous command was user-aborted; there may or may not be a leftover Vitest/node process from that aborted run. If cleanup is needed, identify exact PIDs from a new known command and avoid broad node cleanup.
- `.tmp-operations-rollout-dev.err.log` and `.tmp-operations-rollout-dev.out.log` contain stale dev-runner/runtime errors and should not be treated as current test output without checking timestamps.

<context>
The work had shifted from implementation to verification. The important recovery changes are small and targeted:

1. `packages/db/src/test-embedded-postgres.ts` now returns unsupported immediately on Windows unless `PAPERCLIP_ENABLE_EMBEDDED_POSTGRES_TESTS=true`; `PAPERCLIP_SKIP_EMBEDDED_POSTGRES_TESTS=true` also forces skip.
2. `packages/shared/src/rt2-graph.test.ts` now reflects the expanded RT2 graph contract.

The remaining problem is not TypeScript or the RT2 route tests. It is a server test hang, very likely in one of the workspace provisioning tests.
</context>

<next_action>
Start with:

```sh
pnpm exec vitest run server/src/__tests__/workspace-runtime.test.ts -t "runs a configured provision" --reporter=verbose
```

If it hangs, inspect the test block around `server/src/__tests__/workspace-runtime.test.ts` line 688 and the implementation paths it exercises in `server/src/services/workspace-runtime.ts`. Keep runs single-test filtered until the exact hang is known.
</next_action>
