---
phase: 81-wikillm-graphify-knowledge-projection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - WIKI-01
  - WIKI-02
  - WIKI-03
must_haves:
  truths:
    - "WIKI-01: `rt2KnowledgeProjectorService.projectWikiForCompany` is the canonical wiki page materialization path emitting `index.md`, `log.md`, `topic`, `project`, `schema` types with preserved `Rt2WikiPageProvenance` and `Rt2WikiPageUpdateEvidence`."
    - "WIKI-02: Graphify v3 corpus graph is entirely separate from `rt2_v33_graph_*` product graph tables, using `rt2CorpusGraphService` with SHA256 cache ingest, deterministic extraction, `connected_components_fallback` clustering, and 7 explicit query APIs."
    - "WIKI-03: Zero Paperclip legacy patterns (`WorkQueue`, `AgentTask`, `createIssue`) exist in RT2-native knowledge/graph surfaces; all use `rt2-*` naming."
  artifacts:
    - path: packages/shared/src/types/rt2-knowledge.ts
      provides: Canonical vocabulary for wiki pages and provenance
      exports: Rt2WikiPageType, Rt2WikiPageProvenance, Rt2WikiPageUpdateEvidence
    - path: server/src/services/rt2-knowledge-projector.ts
      provides: Canonical materialization path for wiki pages
      exports: projectWikiForCompany
    - path: server/src/services/rt2-corpus-graph.ts
      provides: Separated sidecar corpus graph operations
      exports: ingestSources, getStats, getReport, getNode, getNeighbors, getCommunity, getGodNodes, getShortestPath
    - path: packages/db/src/schema/rt2_v33_graph_projection.ts
      provides: Separated DB schema for corpus graph sidecar
      exports: rt2V33CorpusGraphSources, rt2V33CorpusGraphNodes, rt2V33CorpusGraphEdges
  key_links:
    - from: server/src/services/rt2-knowledge-projector.ts:projectWikiForCompany
      to: packages/db/src/schema/rt2_v33_wiki_pages.ts
      via: Writes canonical page types with full provenance and update evidence
      pattern: "projectWikiForCompany.*Rt2WikiPageType"
    - from: server/src/services/rt2-corpus-graph.ts
      to: packages/db/src/schema/rt2_v33_graph_projection.ts
      via: Only interacts with corpus graph tables, never with product graph tables
      pattern: "rt2V33CorpusGraph"
---

<objective>
Verify RT2 knowledge projection integration for wikiLLM (WIKI-01) and Graphify v3 Corpus Graph Sidecar (WIKI-02), while proving zero Paperclip legacy patterns remain in these services (WIKI-03). This phase does not introduce new features; it verifies the decisions documented in 81-CONTEXT.md using grep and test runner assertions.
</objective>

<execution_context>
@C:/Users/고상진/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-wikillm-graphify-knowledge-projection/81-CONTEXT.md
@.planning/phases/68-wikillm-living-memory-workflow/68-CONTEXT.md
@.planning/phases/69-graphify-v3-corpus-graph-sidecar/69-CONTEXT.md
@packages/shared/src/types/rt2-knowledge.ts
@server/src/services/rt2-knowledge-projector.ts
@server/src/services/rt2-corpus-graph.ts
@packages/db/src/schema/rt2_v33_wiki_pages.ts
@packages/db/src/schema/rt2_v33_graph_projection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WIKI-01 — wikiLLM Living Memory Integration</name>
  <files>packages/shared/src/types/rt2-knowledge.ts, server/src/services/rt2-knowledge-projector.ts</files>
  <read_first>
packages/shared/src/types/rt2-knowledge.ts
server/src/services/rt2-knowledge-projector.ts
  </read_first>
  <action>
**Verify WIKI-01: Confirm canonical materialization path, vocabulary, and provenance tracking.**

1. **Verify Canonical Path (D-01):**
   Grep `server/src/services/rt2-knowledge-projector.ts` for `projectWikiForCompany`.
   Ensure it is the single entry point for materializing wiki pages from domain events.

2. **Verify Vocabulary (D-02):**
   Grep `packages/shared/src/types/rt2-knowledge.ts` for `Rt2WikiPageType`.
   Ensure it exactly contains `"index" | "log" | "topic" | "project" | "schema"`.

3. **Verify Provenance and Evidence (D-03, D-04):**
   Grep `packages/shared/src/types/rt2-knowledge.ts` for `Rt2WikiPageProvenance` and `Rt2WikiPageUpdateEvidence`.
   Ensure the types exist and match the shapes specified in decisions.

4. **Verify Confidence Vocabulary (D-05):**
   Grep `packages/shared/src/types/rt2-knowledge.ts` for `Rt2WikiConfidenceLabel`.
   Ensure it reuses `"EXTRACTED" | "INFERRED" | "AMBIGUOUS"`.
  </action>
  <verify>
    <automated>
      grep -c "projectWikiForCompany" server/src/services/rt2-knowledge-projector.ts &&
      grep -E "Rt2WikiPageType.*index.*log.*topic.*project.*schema" packages/shared/src/types/rt2-knowledge.ts | wc -l &&
      grep -c "interface Rt2WikiPageProvenance" packages/shared/src/types/rt2-knowledge.ts &&
      grep -c "interface Rt2WikiPageUpdateEvidence" packages/shared/src/types/rt2-knowledge.ts &&
      grep -E "Rt2WikiConfidenceLabel.*EXTRACTED.*INFERRED.*AMBIGUOUS" packages/shared/src/types/rt2-knowledge.ts | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms projectWikiForCompany exists.
    - grep confirms Rt2WikiPageType defines all 5 types.
    - grep confirms Rt2WikiPageProvenance exists.
    - grep confirms Rt2WikiPageUpdateEvidence exists.
    - grep confirms Rt2WikiConfidenceLabel uses unified graph vocabulary.
  </acceptance_criteria>
  <done>WIKI-01 verified: wikiLLM projection uses projectWikiForCompany and follows Rt2WikiPageType vocabulary with proper provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Verify WIKI-02 — Graphify v3 Corpus Graph Completeness</name>
  <files>server/src/services/rt2-corpus-graph.ts, packages/db/src/schema/rt2_v33_graph_projection.ts</files>
  <read_first>
server/src/services/rt2-corpus-graph.ts
packages/db/src/schema/rt2_v33_graph_projection.ts
  </read_first>
  <action>
**Verify WIKI-02: Confirm sidecar separation, ingestion strategy, clustering, and APIs.**

1. **Verify Sidecar Separation (D-06):**
   Grep `packages/db/src/schema/rt2_v33_graph_projection.ts` to confirm corpus graph tables (`rt2_v33_corpus_graph_nodes`, `rt2_v33_corpus_graph_edges`, etc.) exist.
   Grep `server/src/services/rt2-corpus-graph.ts` to ensure it uses the `corpus` tables and not product graph tables.

2. **Verify Incremental Ingest Strategy (D-07):**
   Grep `server/src/services/rt2-corpus-graph.ts` for `ingestSources` and `skippedSources` to confirm SHA256-based skip logic is present.

3. **Verify Clustering Fallback (D-09):**
   Grep `server/src/services/rt2-corpus-graph.ts` for `connected_components_fallback` to ensure it is the explicitly named algorithm.

4. **Verify Query APIs (D-10):**
   Grep `server/src/services/rt2-corpus-graph.ts` for the 7 query APIs:
   `getNode`, `getNeighbors`, `getCommunity`, `getGodNodes`, `getShortestPath`, `getStats`, `getReport`.
  </action>
  <verify>
    <automated>
      grep -c "rt2_v33_corpus_graph_nodes" packages/db/src/schema/rt2_v33_graph_projection.ts &&
      grep -c "ingestSources" server/src/services/rt2-corpus-graph.ts &&
      grep -c "skippedSources" server/src/services/rt2-corpus-graph.ts &&
      grep -c "connected_components_fallback" server/src/services/rt2-corpus-graph.ts &&
      grep -E "getNode\(|getNeighbors\(|getCommunity\(|getGodNodes\(|getShortestPath\(|getStats\(|getReport\(" server/src/services/rt2-corpus-graph.ts | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms corpus graph schema tables exist.
    - grep confirms ingestSources uses skip logic.
    - grep confirms connected_components_fallback algorithm is named explicitly.
    - grep confirms all 7 corpus graph query APIs exist in rt2-corpus-graph.ts.
  </acceptance_criteria>
  <done>WIKI-02 verified: Corpus graph uses separated sidecar tables, SHA256 ingest caching, connected_components_fallback, and exposes all 7 query APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Verify WIKI-03 — RT2-Native Operation & No Paperclip Residue</name>
  <files>server/src/services/rt2-knowledge-projector.ts, server/src/services/rt2-corpus-graph.ts</files>
  <read_first>
server/src/services/rt2-knowledge-projector.ts
server/src/services/rt2-corpus-graph.ts
  </read_first>
  <action>
**Verify WIKI-03: Ensure knowledge and graph services contain zero legacy Paperclip patterns.**

1. **Verify No Legacy Patterns (D-14):**
   Grep `rt2-knowledge-projector.ts` and `rt2-corpus-graph.ts` for legacy Paperclip concepts like `WorkQueue`, `AgentTask`, or `createIssue`.
   Ensure the output is 0.
   
2. **Verify RT2 Namespacing:**
   Verify the services are heavily adopting `rt2-` terminology.
  </action>
  <verify>
    <automated>
      (grep -c "WorkQueue\|AgentTask\|createIssue" server/src/services/rt2-knowledge-projector.ts server/src/services/rt2-corpus-graph.ts || echo "0")
    </automated>
  </verify>
  <acceptance_criteria>
    - grep confirms ZERO instances of WorkQueue, AgentTask, or createIssue in rt2-knowledge-projector.ts and rt2-corpus-graph.ts.
  </acceptance_criteria>
  <done>WIKI-03 verified: Zero legacy Paperclip patterns found in knowledge and graph RT2 surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extractor → Database | Deterministic extraction writes to Corpus Graph sidecar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Tampering | rt2_v33_corpus_graph | mitigate | Corpus graph DB schema explicitly separated from product graph (`rt2_v33_graph_*`). Only `rt2CorpusGraphService` interacts with it. |
| T-81-02 | Spoofing | wikiLLM provenance | mitigate | Required `sourceEventIds` and `Rt2WikiPageProvenance` structured metadata prevents anonymous or fake generation. |
| T-81-03 | Denial | ingestSources | mitigate | SHA256 caching prevents re-ingestion loops for unchanged source documents. |
</threat_model>

<verification>
Run the following in sequence:

```bash
# 1. TypeScript type check
pnpm typecheck

# 2. Check test suites
pnpm exec vitest run server/src/__tests__/rt2-corpus-graph.test.ts
pnpm exec vitest run server/src/__tests__/rt2-knowledge-projector.test.ts
```
</verification>

<success_criteria>
- [ ] grep confirms projectWikiForCompany is the wiki generator
- [ ] grep confirms Rt2WikiPageType has 5 specific types
- [ ] grep confirms Rt2WikiPageProvenance exists
- [ ] grep confirms ingestSources skip logic
- [ ] grep confirms 7 query APIs in rt2-corpus-graph.ts
- [ ] grep confirms ZERO legacy Paperclip patterns in knowledge/graph services
- [ ] pnpm typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/81-wikillm-graphify-knowledge-projection/81-01-SUMMARY.md`
</output>
