---
phase: 50-work-card-editing-and-board-controls
plan: 03
type: execute
wave: 2
depends_on: [50-01, 50-02]
files_modified:
  - server/src/routes/rt2-daily-report.ts
  - server/src/services/rt2-daily-report.ts
  - server/src/services/rt2-work-board.ts
  - server/src/__tests__/rt2-daily-report-routes.test.ts
  - server/src/__tests__/rt2-task-routes.test.ts
autonomous: true
requirements: [BOARD-04, BOARD-05]
requirements_addressed: [BOARD-04, BOARD-05]
must_haves:
  truths:
    - "Operators can persist quick edit fields through canonical server owners"
    - "Daily lane/status saves still materialize activity and wiki evidence"
    - "Daily board payload returns cohesive metadata for filters, search, and sort"
  artifacts:
    - path: "server/src/routes/rt2-daily-report.ts"
      provides: "narrow board-owned update routes"
    - path: "server/src/services/rt2-daily-report.ts"
      provides: "cohesive daily board payload and daily-owned persistence"
    - path: "server/src/services/rt2-work-board.ts"
      provides: "reused quality/price-style metadata where appropriate"
  key_links:
    - from: "server/src/routes/rt2-daily-report.ts"
      to: "packages/shared/src/validators/rt2-daily-report.ts"
      via: "validate(...) middleware"
      pattern: "updateRt2Daily"
    - from: "server/src/services/rt2-daily-report.ts"
      to: "issue_work_products.metadata.rt2BasePrice"
      via: "deliverable/base price persistence"
      pattern: "rt2BasePrice"
    - from: "server/src/services/rt2-daily-report.ts"
      to: "materializeDailyWikiPage"
      via: "saveDailyCard path"
      pattern: "materializeDailyWikiPage"
---

<objective>
Implement server-side Phase 50 edit ownership and enriched daily board payloads.

Purpose: Quick edit must update canonical issue/task/deliverable/quality/OKR sources while daily board controls receive cohesive metadata without a second read model.
Output: Validated routes/services for quick edit fields and daily board cards enriched for filters/search/sort.
</objective>

<execution_context>
@C:/Users/고상진/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/50-work-card-editing-and-board-controls/50-CONTEXT.md
@.planning/phases/50-work-card-editing-and-board-controls/50-RESEARCH.md
@.planning/phases/50-work-card-editing-and-board-controls/50-VALIDATION.md
@.planning/phases/50-work-card-editing-and-board-controls/50-01-SUMMARY.md
@.planning/phases/50-work-card-editing-and-board-controls/50-02-SUMMARY.md

<interfaces>
Existing server ownership points:

```typescript
// server/src/services/rt2-daily-report.ts
export function rt2DailyReportService(db: Db) {
  async function listCards(companyId: string, projectId: string, userId: string, reportDate: string) { ... }
  async function saveDailyCard(companyId, todoIssueId, actorUserId, input) { ... }
}

// server/src/services/rt2-work-board.ts
export function rt2WorkBoardService(db: Db) {
  getBoardOverview(companyId: string, issueIds: string[]): Promise<Rt2BoardOverview>
  updateCard(companyId: string, issueId: string, actorUserId: string, input: UpdateRt2BoardCard): Promise<Rt2BoardCardMeta>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich daily board payload from canonical sources</name>
  <files>server/src/services/rt2-daily-report.ts, server/src/__tests__/rt2-daily-report-routes.test.ts, server/src/__tests__/rt2-task-routes.test.ts</files>
  <behavior>
    - `listDailyBoard`/card construction returns enriched fields from Plan 02 without introducing a second daily board read model per D-20.
    - Deliverable title/type/required/base price come from existing RT2 work product metadata and preserve `basePriceTotal` consistency per D-06 and D-07.
    - Quality status maps to `Rt2BoardQualityStatus`, including `pending_review` and `needs_work`, while retaining current display semantics per D-08 and D-14.
    - OKR metadata exposes direct task goal/profile relation and inherited project fallback distinctly per D-09.
    - Filter metadata supports today/reportDate, actor/assignee, missing deliverable, approval waiting safe proxy, quality issue, search targets, updated/due/price sort per D-12 through D-18.
  </behavior>
  <action>Extend service queries/mapping in place. Reuse `rt2WorkBoardService.getBoardOverview` where it supplies due/quality/price metadata per D-21, but return a cohesive daily card payload so the UI does not stitch inconsistent models. Document the `승인 대기` proxy in test names or assertions if no formal approval join exists per D-15.</action>
  <verify>
    <automated>pnpm exec vitest run server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-task-routes.test.ts packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Daily board route returns all control/edit metadata, old daily report tests remain green, and daily board payload tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validated quick-edit update routes with canonical ownership</name>
  <files>server/src/routes/rt2-daily-report.ts, server/src/services/rt2-daily-report.ts, server/src/services/rt2-work-board.ts, server/src/__tests__/rt2-daily-report-routes.test.ts, server/src/__tests__/rt2-task-routes.test.ts</files>
  <behavior>
    - Title edits update the underlying To-Do issue title through the existing issue/task route/service pattern, not a daily-only override, per D-05.
    - Lane/status edits continue through `saveDailyCard` and preserve activity log plus daily wiki materialization per D-04 and D-28.
    - Deliverable title/type/required/base price edits upsert/update RT2 work product metadata and keep daily `basePriceTotal`/Gold summary consistent per D-06 and D-07.
    - Quality edits reuse `Rt2BoardQualityStatus` and work-board metadata conventions per D-08 and D-21.
    - OKR edits update/clear the task-level profile goal relation and preserve inherited project fallback display per D-09.
    - Each field returns enough updated card metadata for field-level UI feedback; one failed field must not imply all fields saved per D-03.
  </behavior>
  <action>Add narrow route handlers in `rt2-daily-report.ts` using Plan 02 validators and existing board actor/company access patterns. Keep paths board-owned and specific. Enforce company scoping and actor ownership on every mutation. Do not create new deliverable tables, global assignee models, personal preference storage, or Phase 51/52 surfaces.</action>
  <verify>
    <automated>pnpm exec vitest run server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-task-routes.test.ts packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Route tests pass for title, lane/status, deliverable/base price, quality, OKR, cross-company rejection, wrong-assignee rejection, invalid payload rejection, and wiki/activity preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route→service | Validated request bodies enter mutation services |
| service→database | Mutations touch issues, daily cards, work products, work-board cards, and task profiles |
| actor→company/card | Board actor identity must constrain company and card ownership |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-50-08 | Medium | Tampering | quick edit routes | mitigate | Use shared Zod validators and reject unknown/invalid payloads before service mutation. |
| T-50-09 | High | Elevation of Privilege | daily card service | mitigate | Preserve board actor checks and assignee ownership checks for every daily-card-owned mutation. |
| T-50-10 | High | Tampering | cross-company data access | mitigate | Keep service queries scoped by `companyId`; route tests must prove foreign company ids fail. |
| T-50-11 | Medium | Repudiation | lane/status edits | mitigate | Preserve daily activity log and wiki materialization for daily lane/status changes. |

## Block-On-High Gate

Unresolved or merely accepted High/Critical threats block execution. `T-50-09` and `T-50-10` require passing route tests for ownership and company scoping before this plan can be summarized as complete, unless a human override is recorded in the summary.
</threat_model>

<verification>
```sh
pnpm exec vitest run server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-task-routes.test.ts packages/shared/src/rt2-daily-report.test.ts
pnpm typecheck
```
</verification>

<success_criteria>
- BOARD-04 fields persist through canonical owners.
- BOARD-05 metadata is available in a cohesive daily board payload.
- D-03 through D-22 and D-27 through D-28 are implemented at server/shared boundaries.
- Expected security threats are mitigated with route tests.
</success_criteria>

<output>
After completion, create `.planning/phases/50-work-card-editing-and-board-controls/50-03-SUMMARY.md`.
</output>
