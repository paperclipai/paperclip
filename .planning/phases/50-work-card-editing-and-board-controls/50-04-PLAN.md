---
phase: 50-work-card-editing-and-board-controls
plan: 04
type: execute
wave: 3
depends_on: [50-01, 50-02, 50-03]
files_modified:
  - ui/src/components/Rt2DailyBoard.tsx
  - ui/src/components/Rt2DailyBoard.test.tsx
  - ui/src/api/rt2-daily-report.ts
autonomous: true
requirements: [BOARD-04, BOARD-05]
requirements_addressed: [BOARD-04, BOARD-05]
must_haves:
  truths:
    - "Operators edit title, lane/status, deliverable, base price, quality, and OKR inside the daily board"
    - "Operators filter by today, mine, missing deliverable, approval waiting, and quality issue without losing lane grouping"
    - "Search and sort are view-only and do not mutate persisted lane/status or manual order"
  artifacts:
    - path: "ui/src/components/Rt2DailyBoard.tsx"
      provides: "board toolbar, quick edit surface, field-level feedback"
    - path: "ui/src/components/Rt2DailyBoard.test.tsx"
      provides: "component regression coverage for Phase 50"
    - path: "ui/src/api/rt2-daily-report.ts"
      provides: "client calls used by the component"
  key_links:
    - from: "ui/src/components/Rt2DailyBoard.tsx"
      to: "ui/src/api/rt2-daily-report.ts"
      via: "quick edit mutations"
      pattern: "update.*Daily"
    - from: "ui/src/components/Rt2DailyBoard.tsx"
      to: "Rt2DailyReportCard"
      via: "enriched metadata rendering and filtering"
      pattern: "deliverable|quality|okr|approval"
---

<objective>
Implement the Phase 50 daily board UI: quick edit, field-level feedback, filters, search, and sort.

Purpose: Operators should repeatedly manipulate daily work from the board context without deep navigation or losing lane state.
Output: Dense Korean-first `Rt2DailyBoard` controls and passing focused UI/integration verification.
</objective>

<execution_context>
@C:/Users/고상진/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/49-daily-work-kanban-core/49-CONTEXT.md
@.planning/phases/50-work-card-editing-and-board-controls/50-CONTEXT.md
@.planning/phases/50-work-card-editing-and-board-controls/50-UI-SPEC.md
@.planning/phases/50-work-card-editing-and-board-controls/50-VALIDATION.md
@.planning/phases/50-work-card-editing-and-board-controls/50-01-SUMMARY.md
@.planning/phases/50-work-card-editing-and-board-controls/50-02-SUMMARY.md
@.planning/phases/50-work-card-editing-and-board-controls/50-03-SUMMARY.md

<interfaces>
UI contracts:

```typescript
// ui/src/api/rt2-daily-report.ts
rt2DailyReportApi.getBoard(...)
rt2DailyReportApi.saveCard(...) // lane/status daily path remains
// Plan 02 adds typed helpers for title, deliverable/base price, quality, OKR.

// Product copy required by UI-SPEC
"오늘 업무" | "내 업무" | "산출물 누락" | "승인 대기" | "품질 이슈"
"기본 순서" | "최근 수정순" | "마감일순" | "보완 필요 먼저" | "품질 이슈 먼저" | "Gold 높은순"
"저장중" | "저장됨" | "저장 실패" | "다시 시도"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scan-first card quick edit with independent field feedback</name>
  <files>ui/src/components/Rt2DailyBoard.tsx, ui/src/components/Rt2DailyBoard.test.tsx, ui/src/api/rt2-daily-report.ts</files>
  <behavior>
    - Quick edit stays inside `daily-work` board context and opens only on operator intent per D-01 and D-02.
    - Editable fields are exactly title, lane/status, deliverable, base price, quality, and OKR per BOARD-04.
    - Field-level save states show Korean `저장중`, `저장됨`, `저장 실패`, and retry affordance near the edited card per D-03 and D-23.
    - Lane/status select still uses the daily report save path per D-04.
    - Title, deliverable/base price, quality, and OKR use the typed helpers from Plan 02/03 and update card badges/summary from returned metadata per D-05 through D-09.
    - Default card remains scan-first with compact metadata visible without expansion per D-24 and D-25.
    - Update focused `Rt2DailyBoard` tests for these quick edit controls and Korean save/failure states per D-26.
  </behavior>
  <action>Update `Rt2DailyBoard.tsx` using existing dense card patterns and lucide/shadcn-style controls already in the app. Use Korean labels from the UI-SPEC. Preserve drafts on failed saves and allow retry. Do not add deep issue-detail navigation, nested cards inside cards, large explanatory copy, delete actions, One-Liner capture promotion, mobile/native draft review, or Jarvis/wiki/graph/economy evidence panels.</action>
  <verify>
    <automated>pnpm exec vitest run ui/src/components/Rt2DailyBoard.test.tsx packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Component tests pass for quick edit affordance, all six editable fields, independent Korean feedback, failed draft preservation, and daily lane save callback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add filter, search, and view-only sort controls</name>
  <files>ui/src/components/Rt2DailyBoard.tsx, ui/src/components/Rt2DailyBoard.test.tsx</files>
  <behavior>
    - Toolbar includes visible composable chips `오늘 업무`, `내 업무`, `산출물 누락`, `승인 대기`, and `품질 이슈` per D-10.
    - Filters apply across all lanes while preserving `할 일 / 진행 중 / 완료` grouping and active-filter empty lane copy per D-11.
    - `오늘 업무`, `내 업무`, `산출물 누락`, `승인 대기`, and `품질 이슈` semantics follow D-12 through D-15.
    - Search matches visible card text and key metadata per D-16.
    - Sort modes follow D-18 and are view-only; no save call, lane/status mutation, or persisted order mutation occurs per D-17.
    - Control state survives normal board refresh/rerender in the current session per D-19.
    - Toolbar uses fixed-height/stable compact controls, aria labels, `aria-pressed`, and Korean accessible labels per UI-SPEC and D-25.
  </behavior>
  <action>Implement board controls in `Rt2DailyBoard.tsx` as client-side view state over the current board payload unless a field is absent, in which case use only the enriched metadata from Plan 03. Keep empty lanes visible. Keep default sort as server/current board order. Do not persist user preferences unless an existing local in-memory component pattern already exists; no new preference storage.</action>
  <verify>
    <automated>pnpm exec vitest run ui/src/components/Rt2DailyBoard.test.tsx</automated>
  </verify>
  <done>Component tests pass for all required filters, search targets, sort modes, lane grouping preservation, no save-on-sort, and session refresh state retention.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused phase verification and document host constraints</name>
  <files>ui/src/components/Rt2DailyBoard.test.tsx, server/src/__tests__/rt2-daily-report-routes.test.ts, server/src/__tests__/rt2-task-routes.test.ts, packages/shared/src/rt2-daily-report.test.ts</files>
  <action>Run the Phase 50 focused verification from `50-VALIDATION.md`: `pnpm typecheck` and the focused Vitest suite. Then run `pnpm test` if feasible on the host. If Windows embedded Postgres or broader test timeout blocks full-suite evidence, record the focused green commands and the exact skip/timeout evidence in the plan summary per D-29.</action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm exec vitest run ui/src/components/Rt2DailyBoard.test.tsx server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-task-routes.test.ts packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Focused suite and typecheck pass, or any host limitation is recorded with exact command/output evidence; `pnpm test` is run if feasible and not used as a blocking default when host constraints match STATE.md accepted debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→React state | Edited text/search values enter component state |
| React state→API helper | Quick edit payloads are sent to typed server endpoints |
| server response→UI render | Edited metadata returns to card badges and searchable text |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-50-12 | Medium | Tampering | quick edit UI payloads | mitigate | Send only typed field payloads through Plan 02 helpers; do not construct generic mutation bodies. |
| T-50-13 | Medium | Information Disclosure/Tampering | edited title/search rendering | mitigate | Render user-provided values as React text; do not use `dangerouslySetInnerHTML` or raw HTML interpolation. |
| T-50-14 | Low | Denial of Service | client-side filtering | accept | Current board payload is assumed small enough per research A1; filters operate on in-memory current board only. |
| T-50-15 | Medium | Tampering | sort/search controls | mitigate | Sort/search are view-only and tests assert they do not call persistence callbacks. |

## Block-On-High Gate

Unresolved or merely accepted High/Critical threats block execution. This plan currently has no High/Critical threats; if execution discovers one, it must add the threat, mitigation, and passing test before proceeding unless a human override is recorded in the summary.
</threat_model>

<verification>
```sh
pnpm typecheck
pnpm exec vitest run ui/src/components/Rt2DailyBoard.test.tsx server/src/__tests__/rt2-daily-report-routes.test.ts server/src/__tests__/rt2-task-routes.test.ts packages/shared/src/rt2-daily-report.test.ts
pnpm test
```

Run `pnpm test` when feasible; record focused evidence if the Windows host constraints documented in `.planning/STATE.md` block broad execution.
</verification>

<success_criteria>
- Operators can edit title, lane/status, deliverable, base price, quality state, and OKR badge from the board context.
- Filters for today, mine, missing deliverable, approval waiting, and quality issue work consistently across lanes.
- Sort/search controls preserve lane state and never mutate persisted board order.
- Product copy remains Korean-first and RealTycoon2-facing.
- D-01 through D-29 are satisfied or explicitly preserved as constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/50-work-card-editing-and-board-controls/50-04-SUMMARY.md`.
</output>
