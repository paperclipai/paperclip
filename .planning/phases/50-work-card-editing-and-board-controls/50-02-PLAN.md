---
phase: 50-work-card-editing-and-board-controls
plan: 02
type: execute
wave: 1
depends_on: [50-01]
files_modified:
  - packages/shared/src/types/rt2-daily-report.ts
  - packages/shared/src/validators/rt2-daily-report.ts
  - packages/shared/src/rt2-daily-report.test.ts
  - ui/src/api/rt2-daily-report.ts
autonomous: true
requirements: [BOARD-04, BOARD-05]
requirements_addressed: [BOARD-04, BOARD-05]
must_haves:
  truths:
    - "Daily board payload exposes the metadata needed for quick edit and board controls"
    - "Quick edit payloads are narrow, validated, and owned by the daily board contract"
    - "Client API helpers use typed shared contracts"
  artifacts:
    - path: "packages/shared/src/types/rt2-daily-report.ts"
      provides: "enriched daily card and edit payload types"
    - path: "packages/shared/src/validators/rt2-daily-report.ts"
      provides: "Zod validators for board-owned edit routes"
    - path: "ui/src/api/rt2-daily-report.ts"
      provides: "typed API helpers for Phase 50 daily-board edits"
  key_links:
    - from: "packages/shared/src/validators/rt2-daily-report.ts"
      to: "server/src/routes/rt2-daily-report.ts"
      via: "route validate(...) middleware in later plan"
      pattern: "updateRt2Daily.*Schema"
    - from: "ui/src/api/rt2-daily-report.ts"
      to: "packages/shared/src/validators/rt2-daily-report.ts"
      via: "shared request type imports"
      pattern: "UpdateRt2Daily"
---

<objective>
Define the shared data contracts and typed client helpers for Phase 50 quick edit and board controls.

Purpose: Later server and UI work needs a single cohesive daily board payload and narrow edit contracts instead of ad hoc client-side stitching.
Output: Extended `Rt2DailyReportCard` metadata, new Zod validators/types for edit payloads, and typed UI API helpers.
</objective>

<execution_context>
@C:/Users/고상진/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/50-work-card-editing-and-board-controls/50-CONTEXT.md
@.planning/phases/50-work-card-editing-and-board-controls/50-RESEARCH.md
@.planning/phases/50-work-card-editing-and-board-controls/50-UI-SPEC.md
@.planning/phases/50-work-card-editing-and-board-controls/50-VALIDATION.md
@.planning/phases/50-work-card-editing-and-board-controls/50-01-SUMMARY.md

<interfaces>
Contracts this plan must preserve or extend:

```typescript
export type Rt2DailyLane = "todo" | "doing" | "done";
export type Rt2BoardQualityStatus = "none" | "pending_review" | "reviewed" | "needs_work";
export const rt2DailyReportDateSchema = z.string().regex(...);
export const upsertRt2DailyReportCardSchema = z.object({
  projectId: z.string().uuid(),
  reportDate: rt2DailyReportDateSchema,
  lane: rt2DailyLaneSchema,
  bucketLabel: z.string().trim().max(40).nullable().optional(),
  progressPercent: z.number().int().min(0).max(100),
  note: z.string().trim().max(500).nullable().optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend daily board shared types for cohesive card metadata</name>
  <files>packages/shared/src/types/rt2-daily-report.ts, packages/shared/src/rt2-daily-report.test.ts</files>
  <behavior>
    - `Rt2DailyReportCard` exposes minimum fields for D-06, D-07, D-08, D-09, D-12, D-13, D-14, D-15, D-16, D-18, and D-20 without creating a second board read model.
    - Add typed nested metadata for deliverable edit/display: id when available, title, type, required flag, owner/source, base price, submitted count, missing flag.
    - Add richer quality field using `Rt2BoardQualityStatus` vocabulary while preserving backward-compatible daily summary labels where needed.
    - Add OKR metadata that distinguishes direct task OKR from inherited project fallback: direct goal id/title nullable, inherited goal id/title nullable, source enum such as `direct_task | inherited_project | none`.
    - Add filter/search/sort metadata: report date match, actor/assignee match, approval waiting/proxy with documented source, due date nullable, updated timestamp nullable, searchable labels.
  </behavior>
  <action>Update types and shared tests to satisfy Wave 0 contract tests. Do not add Phase 51/52 fields. Keep names RealTycoon2/work-board specific and avoid Paperclip-facing terminology. Use optional/null fields where existing data may not be available to avoid broad schema churn.</action>
  <verify>
    <automated>pnpm exec vitest run packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Shared daily board types compile conceptually for all required quick edit/control metadata and Wave 0 shared metadata tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add narrow quick-edit validators and client API helpers</name>
  <files>packages/shared/src/validators/rt2-daily-report.ts, packages/shared/src/rt2-daily-report.test.ts, ui/src/api/rt2-daily-report.ts</files>
  <behavior>
    - Validators cover board-owned edit endpoints from D-22: title, daily status/lane, deliverable/base price, quality, and task OKR.
    - Title validator trims and rejects empty titles with a max length consistent with existing issue validation.
    - Deliverable validator supports title/type/required/base price and rejects negative price per UI-SPEC validation copy.
    - Quality validator accepts only `none`, `pending_review`, `reviewed`, and `needs_work` per D-08.
    - OKR validator accepts a UUID goal id or explicit clear/null intent per D-09.
    - Client helpers call narrow daily-board-owned routes and use shared request/response types; status/lane can continue calling the existing `saveCard` helper per D-04.
  </behavior>
  <action>Create exports in `packages/shared/src/validators/rt2-daily-report.ts` and corresponding inferred types. Add UI API helper functions in `ui/src/api/rt2-daily-report.ts` for later `Rt2DailyBoard` use. Do not implement server handlers here. Keep endpoint path segments English and product-facing error/copy out of the API helper.</action>
  <verify>
    <automated>pnpm exec vitest run packages/shared/src/rt2-daily-report.test.ts</automated>
  </verify>
  <done>Validator tests pass, invalid payload tampering is rejected, and typed client helpers exist for quick edit fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→shared schema | User-editable payloads are constructed in the browser before API submission |
| shared schema→server routes | Validators become the contract that server routes must enforce |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-50-05 | Medium | Tampering | quick edit validators | mitigate | Zod schemas enforce enum, UUID, max length, nullability, and non-negative price constraints. |
| T-50-06 | Medium | Information Disclosure | enriched card payload | mitigate | Add only fields required for board operation; do not expose unrelated Jarvis/wiki/graph/economy evidence. |
| T-50-07 | Medium | Tampering | client API helpers | mitigate | Helpers use typed payloads and narrow endpoints instead of generic patch bodies. |

## Block-On-High Gate

Unresolved or merely accepted High/Critical threats block execution. This plan currently has no High/Critical threats; if execution discovers one, it must add the threat, mitigation, and passing test before proceeding unless a human override is recorded in the summary.
</threat_model>

<verification>
```sh
pnpm exec vitest run packages/shared/src/rt2-daily-report.test.ts
pnpm typecheck
```
</verification>

<success_criteria>
- BOARD-04 edit fields have shared validated payloads.
- BOARD-05 control metadata is present on the daily board card contract.
- D-06 through D-22 data ownership decisions are reflected in type/validator shape.
- No implementation file outside shared contracts and UI API helper is changed in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/50-work-card-editing-and-board-controls/50-02-SUMMARY.md`.
</output>
