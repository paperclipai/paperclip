# Phase 70 Plan - Economy, Marketplace, P&L, and CareerMate Loop

**Generated:** 2026-05-01T15:13:39+09:00
**Status:** In execution

## Goal

Close `ECON-01`, `ECON-02`, and `ECON-03` by connecting existing Marketplace, P&L, settlement, ledger, quality, and CareerMate/gamification evidence into the primary RealTycoon2 operating loop.

## Tasks

### Task 1 - Shared CareerMate Progression Contract

- Add typed progression inputs/results to `rt2-gamification`.
- Add `deriveRt2CareerProgression` as a deterministic helper using XP, approved settlements, earned ledger gold, finalized quality, achievements, portfolio/milestones, rejected settlements, and high-risk flags.
- Export the new types/helper from shared package barrels.
- Add focused shared tests for ready, review, and missing evidence states.

### Task 2 - Server Read-Model And Route

- Extend `rt2CareerMateService` with `getCareerProgression(companyId, agentId)`.
- Pull existing evidence from career, settlement, ledger, quality, XP, achievement, and balance tables.
- Return evidence counts, tier/level/reputation/avatar state, warnings, and source links without mutating state.
- Add `GET /companies/:companyId/rt2/career/progression/:agentId`.

### Task 3 - Primary Loop UI Links

- Add P&L and Jarvis Marketplace to company primary navigation.
- Extend `Rt2DailyBoard` economy evidence rail with links to `/pnl`, settlement governance, `/marketplace`, and CareerMate/gamification evidence.
- Keep the daily cockpit as the first operating surface.

### Task 4 - CareerMate UI Evidence

- Add a typed client call for CareerMate progression.
- Show a compact CareerMate evidence section inside `Rt2GamificationPanel` when an `agentId` is present.
- Label missing or partial evidence explicitly.

### Task 5 - DevPlan Gate

- Mark `economy-loop` complete only after Task 1-4 anchors exist.
- Update gate tests to assert Phase 70 score/count changes and evidence anchors.

### Task 6 - Verification

- Run focused tests while iterating.
- Run `pnpm typecheck && pnpm test`.

## Acceptance Mapping

- `ECON-01`: primary navigation and daily cockpit links expose Marketplace/P&L with deliverable, quality, gold, and settlement evidence.
- `ECON-02`: settlement proposal/approval/rejection, ledger entry, P&L period, and anti-gaming signals remain visible through existing P&L flows and daily links.
- `ECON-03`: CareerMate progression is derived from ledger/settlement/quality/gamification evidence and displayed as evidence-backed or explicitly partial/missing.
