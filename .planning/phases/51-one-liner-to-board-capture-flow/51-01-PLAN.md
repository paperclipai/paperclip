---
phase: 51-one-liner-to-board-capture-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/types/rt2-task.ts
  - packages/shared/src/validators/rt2-task.ts
  - packages/shared/src/rt2-task.test.ts
  - ui/src/api/rt2-tasks.ts
autonomous: true
requirements: [CAPTURE-01, CAPTURE-02, CAPTURE-03]
requirements_addressed: [CAPTURE-01, CAPTURE-02, CAPTURE-03]
must_haves:
  truths:
    - "Web, floating, and voice One-Liner inputs use the shared capture draft queue"
    - "Mobile/native/inbound sources remain supported by the same validator"
---

<objective>
Extend shared capture contracts so local web/floating/voice One-Liner input can enter the same draft queue as mobile/native/inbound sources.
</objective>

<tasks>
<task type="auto">
  <name>Expand capture source enum</name>
  <files>packages/shared/src/types/rt2-task.ts, packages/shared/src/validators/rt2-task.ts, packages/shared/src/rt2-task.test.ts, ui/src/api/rt2-tasks.ts</files>
  <action>Add `web`, `floating`, and `voice` to shared/UI source types and validator tests.</action>
  <verify>pnpm exec vitest run packages/shared/src/rt2-task.test.ts</verify>
</task>
</tasks>

<verification>
pnpm exec vitest run packages/shared/src/rt2-task.test.ts
</verification>

<success_criteria>
- CAPTURE-01 web capture can create a draft.
- CAPTURE-03 mobile/native/inbound sources remain accepted.
</success_criteria>

