# Phase 23 Plan: Advanced Work Board and Native Capture

**Status:** Complete  
**Created:** 2026-04-25

## Goal

Trello advanced parity와 mobile/native inbound draft queue를 RealTycoon2 업무 보드에 연결한다.

## Tasks

1. RT2 board metadata와 capture draft schema/migration 추가.
2. shared validators/types와 UI API client 확장.
3. server route/service에 board metadata, checklist, attachment, capture queue, promotion, failure audit 구현.
4. `/issues` 업무 보드에 checklist/due/price/quality/attachment preview와 board filter/sort 추가.
5. native capture queue panel을 업무 보드에 연결하고 review promotion/audit action 제공.
6. requirements/state/alignment 문서를 Phase 23 완료로 갱신.
7. typecheck와 route/UI regression test 실행.

## Acceptance

- `TRELLO-03` checklist add/toggle/reorder/progress가 카드에서 보인다.
- `TRELLO-04` due, priority, assignee, attachment preview가 카드/상세 확장 영역에 있다.
- `TRELLO-05` board filter/sort가 lane/assignee/OKR/due/price/quality를 지원한다.
- `CAPTURE-02` inbound draft queue에서 Task/To-Do/Deliverable 승격 route가 있다.
- `CAPTURE-03` source별 duplicate/permission/failure status와 audit trail이 있다.
