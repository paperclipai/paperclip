# Changelog

## 0.3.1 (2026-04-25)

- Initial release for Claude Code local adapter
- Supports Claude Code CLI via `@anthropic-ai/claude-code` npm package
- Session persistence via `--resume <session-id>`
- Stream JSON output parsing for usage tracking
- MCP integration support via paperclip-mcp-server
