/**
 * ntfy.sh Notifier (Example)
 * 
 * This plugin forwards Paperclip domain events to ntfy.sh or custom ntfy servers.
 * It serves as a reference implementation for event-driven automation plugins.
 */

export { default as manifest } from "./manifest.js";
export { default as worker } from "./worker.js";
