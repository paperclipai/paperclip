#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd -- "$(dirname -- "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "[pre-commit] Running docs guardrails..."
python3 scripts/tools/docs-lint.py
python3 scripts/tools/docs-drift-check.py

echo "[pre-commit] Docs guardrails passed."
