# pg_hba.conf override — trust all local/container connections
# TYPE  DATABASE  USER  ADDRESS      METHOD
local   all       all                trust
host    all       all   0.0.0.0/0    trust
host    all       all   ::0/0        trust
