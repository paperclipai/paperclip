DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret

# Discord webhook for daily merge digest (scripts/discord-daily-digest.sh)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# OPC Coach AI Configuration
# Uses Anthropic SDK format. Set API key and optionally override base URL / model.
# OPC_COACH_API_KEY=your-api-key-here
# OPC_COACH_BASE_URL=https://api.anthropic.com  # or any Anthropic-compatible endpoint
# OPC_COACH_MODEL=claude-sonnet-4-20250514      # or any model supported by your endpoint
