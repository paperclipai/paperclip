# Copy to ".env" at repo root. Server loads: instance .env (next to config.json) -> cwd .env -> monorepo root .env (directory with pnpm-workspace.yaml). See docs/项目计划/最佳实践/001-运维-回形针本地.md
#
# 本仓库约定：开发默认使用 Docker Compose 里的 Postgres（docker/docker-compose.yml 的 db），不要使用「未设置 DATABASE_URL → 内嵌库」作日常路径；也不要默认依赖本机全局安装的 Postgres。
# 先执行：docker compose -f docker/docker-compose.yml up -d
#
# `pnpm dev`（受管 dev-runner）默认注入 PAPERCLIP_STRICT_PORTS=true：监听端口被占用时不再自动换端口，应让进程失败或先 dev:stop。若需旧行为：`PAPERCLIP_STRICT_PORTS=false`
# PAPERCLIP_STRICT_PORTS=false
DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret

# 关闭「滞留回收」自动子单（stranded_issue_recovery）：默认开启；设为 false 则不再新建该类子单，修改后需重启 API
# PAPERCLIP_STRANDED_ISSUE_RECOVERY_ENABLED=false

# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
