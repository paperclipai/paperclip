DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret

# Discord webhook for daily merge digest (scripts/discord-daily-digest.sh)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Deterministic run-rate / no-progress cap (WEI-209/WEI-210).
# off = gate disabled, log = evaluate + log only (default), enforce = auto-pause + notify board.
# Switch to "enforce" only after the green test + CTO review.
# PAPERCLIP_RUN_CAPS_MODE=log
