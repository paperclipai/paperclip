DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false

# Observability (optional) — run docker compose -f docker-compose.observability.yml up to start the stack
# PAPERCLIP_METRICS_ENABLED=true          # expose /metrics for Prometheus scraping
# PAPERCLIP_LOKI_URL=http://localhost:3100 # ship logs to Loki via pino-loki
# PAPERCLIP_OTEL_ENDPOINT=http://localhost:4318  # OTLP HTTP endpoint for traces (Tempo)
