{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "codereview.fullname" . }}
  labels:
    {{- include "codereview.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ include "codereview.fullname" . }}-tls
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "codereview.fullname" . }}-api
                port:
                  number: {{ .Values.service.port }}
          - path: /webhook
            pathType: Prefix
            backend:
              service:
                name: {{ include "codereview.fullname" . }}-webhook
                port:
                  number: {{ .Values.service.port }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "codereview.fullname" . }}-frontend
                port:
                  number: {{ .Values.service.port }}
{{- end }}
