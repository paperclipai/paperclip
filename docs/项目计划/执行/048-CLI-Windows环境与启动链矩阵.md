---
status: 待办
---

# CLI Windows/Git Bash 执行面内化与 Plan A 启动矩阵

**母本：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§C3–C4**、§1 目标(5)

## 背景与范围

- 内化预设：`more` 不可用、`/tmp`/`$TEMP`、路径重写；减少模型试错轮次。
- **Plan A**（绕过 `cmd`、直连 `node` 等）：何种场景启用何链路的**矩阵**；与 **`探查/019`**、`035`、`最佳实践/006` 回填交叉链。

## 交付物（可验收）

1. **`最佳实践`** 或 **`docs/项目计划/探查`** 短文一页（可归入计划区）——执行面真值矩阵。
2. 适配层默认 env / preamble（若改代码）或运维检查清单更新。
3. 复现实验：**无 `more`** 环境下单次 `curl` 成功路径不再需要模型自创三轮。

## 依赖与并行

- **并行**：[**047**](047-适配器_stdout解析失败与结构化摘要.md)（可先文档后代码）。

## 验证证据（完成后填写）
