---
status: 待办
---

# 唤起场景契约测试与回归红线（H）

**母本：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§H**

## 背景与范围

- **H1**：最小冒烟集：`comment_wake`⨯`heartbeat` **撞车**、Skill **全开**、stdout **超限**、Windows Bash **缺 more**。
- **H2**：回归红线：**comment_wake p95 tokens**、工具次数、p95 延迟、适配器 **`JSON parse`** 失败率。

## 交付物（可验收）

1. Vitest/集成脚本/手工清单三者之一或多个，**写明如何跑**（入 `pnpm` registry 为佳）。
2. CI 阈值或 「发布前勾选」闸门（任选其一）。
3. 与各上游单 **041–052** 的覆盖矩阵一对多表。

## 依赖与并行

- **殿后**：本单宜在 **047/048** 有重大改动后收口；也可先行写用例草稿 **红色测**。

## 验证证据（完成后填写）
