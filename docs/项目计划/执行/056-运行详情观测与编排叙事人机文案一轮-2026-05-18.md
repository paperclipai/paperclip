---
status: 已完成
---

# 056-运行详情观测与编排叙事·人机文案一轮（2026-05-18）

## 目标

- **运行详情**：减少「API 已终态、界面仍像进行中」与 **英文诊断句** 对操作者的噪音；**事件 / stderr 标题 / lifecycle** 以 zh-CN 为主。  
- **编排叙事**：把 **run 语义、触及多事务、抢单现状** 写成可引用文书，便于与产品讨论对齐。  
- **协作**：落地 **[`019-实践-服务端与适配器人机文案汉化.md`](../最佳实践/019-实践-服务端与适配器人机文案汉化.md)** —— 人类只贴对照文案、助手按层改。

## 交付（路径索引）

| 类 | 路径 / 说明 |
| --- | --- |
| **编排 · run 语义与观测** | [`../编排/编排-run执行语义与观测边界-现状说明.md`](../编排/编排-run执行语义与观测边界-现状说明.md) |
| **编排 · 主单拆子单抢单・修理方向** | [`../编排/编排-主单拆子单同经办串行抢单-现状与修理方向.md`](../编排/编排-主单拆子单同经办串行抢单-现状与修理方向.md) |
| **最佳实践 · 服务端/适配器文案** | [`../最佳实践/019-实践-服务端与适配器人机文案汉化.md`](../最佳实践/019-实践-服务端与适配器人机文案汉化.md)；[`011`](../最佳实践/011-实践-回形针UI页面汉化流程.md) 增 **§7** 指回 **019** |
| **UI · run 详情缓存** | `ui/src/components/HeartbeatRunDetailPanel.tsx`：`mergeHeartbeatRunSnapshot`（`updatedAt` 较新者优先，避免列表真值被旧 `runDetail` 缓存盖住） |
| **UI · i18n** | `ui/src/lib/i18n.ts`：`runLifecycleMessageDisplay` → **`run-lifecycle-legacy-display.ts`**；转写 **`RunTranscriptView.tsx`** |
| **服务端 · lifecycle 真值** | `server/src/services/heartbeat-lifecycle-messages.ts`（2026-05-20 追加） |
| **服务端 · 排队作废理由** | `server/src/services/heartbeat.ts`：**终态 done/cancelled**、重试/续跑抑制、续跑非进行中等 **reason** 中文化；**`issue_terminal_status`** 等 **errorCode** 不变 |
| **服务端 · 对账取消句** | `server/src/services/orchestration-invariants.ts`：`RUN_CANCEL_ISSUE_TERMINAL_WHILE_RUNNING` 中文化 |
| **测试** | `server/src/__tests__/heartbeat-stale-queue-invalidation.test.ts`：断言随文案调整（如 `blocked` / `执行锁` 片段） |

## 验证（当轮）

- `pnpm exec tsc --noEmit -p ui`  
- `pnpm exec vitest run server/src/__tests__/heartbeat-stale-queue-invalidation.test.ts`（及相关单测，以当轮改动为准）

## 说明

- **库里旧事件**仍可能存英文 `message`；界面通过 **`runLifecycleMessageDisplay`** → **`ui/src/lib/run-lifecycle-legacy-display.ts`** 对 **已知旧句式** 做展示映射，**不**改写历史行。  
- **抢单**产品修理 **未**在本轮开事务改 `server` 编排策略，见 [`编排-主单拆子单同经办串行抢单-现状与修理方向.md`](../编排/编排-主单拆子单同经办串行抢单-现状与修理方向.md) **§3**。

---

## 2026-05-20 追加：lifecycle 事件句全路径汉化（060 探查→执行）

### 探查范围

沿 **`heartbeat.ts` → `appendRunEvent`（`eventType: lifecycle`）** 与 **`cancelRunInternal` / 排队作废 `message: reason`** 扫英文；含排期重试闸门、进程丢失、评论跟进唤醒、关单结案等。

### 交付

| 层 | 路径 |
| --- | --- |
| **服务端真值（新事件写中文）** | `server/src/services/heartbeat-lifecycle-messages.ts`；`heartbeat.ts` 引用 |
| **UI 旧库兼容** | `ui/src/lib/run-lifecycle-legacy-display.ts` + `run-lifecycle-legacy-display.test.ts` |
| **单测** | `server/src/__tests__/heartbeat-stale-queue-invalidation.test.ts`（断言改中文片段） |

### 验证（当轮）

- `pnpm exec vitest run ui/src/lib/run-lifecycle-legacy-display.test.ts`
- `pnpm exec vitest run server/src/__tests__/heartbeat-stale-queue-invalidation.test.ts`
- Board：ROU-6 run `101c048a-…` **事件（3）** 已为中文（新 run 走服务端；旧 run 走 legacy 映射）

## 链回

- 编排目录：**[`../编排/README.md`](../编排/README.md)**  
- 索引：**[`../index.md`](../index.md)**「执行 / 最佳实践 / 编排」
