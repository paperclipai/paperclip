---
status: 待办
---

# 唤起类分源计量与台账报表（对齐 015/023）

**母本：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§F1–F3**

## 背景与范围

- **F1**：`heartbeat_timer`、`issue_commented`、`continuation` 等 **分项限额/告警**。
- **F2**：唤起类专用报表：**工具次数、retry、cache-hit** —— 对齐 DeepSeek/paperclip reconcile。
- **F3**：`total_cost_usd` 若为累计语义，适配器 **`delta`/文书口径**收口（沿用 **015** 讨论）。

## 交付物（可验收）

1. 报表/UI 导出或脚本（可归 `pnpm` / `scripts`）；只读可查。
2. 与 **`015`**、`023`、「成本页」需求 gap 对齐表。
3. 若不改代码则本单转为「取证 + 接线说明」须在验证证据写明。

## 依赖与并行

- **可参考**：[`028-成本页`](028-成本页英文UI与中文化建议.md)，[`047`](047-适配器_stdout解析失败与结构化摘要.md)（结构化 usage）。

## 验证证据（完成后填写）
