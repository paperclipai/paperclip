---
status: 待办
---

# 多源唤醒合并与有效触发归因

**母本：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§A2**、§1 目标(3)

## 背景与范围

同一短时窗、`agentId` + `issueId` 上出现 `timer` 与 `automation`（等）多重唤醒时的**合并/防抖**，并在 run **元数据**中落 **effectiveTrigger**（最终采纳原因），可审计。

## 交付物（可验收）

1. 合并策略（优先级、丢弃、延迟）的书面规则 + 示例时间线。
2. DB/API/run 读出字段含 `effectiveTrigger` 或等价物。
3. 活动日志或 run 详情可查证「人类以为一次评论」对应的**真实触发链**。

## 依赖与并行

- **依赖**：调度入口梳理（heartbeat + issues 注释路径）。
- **并行**：可与 **041** 产品枚举并行起草；实现阶段宜在 **041** 之后或与 **043** 同迭代。

## 验证证据（完成后填写）
