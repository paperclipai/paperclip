# 020 — 探查：ROU-44（滞留回收）与 ROU-41 收口链

**状态：** 进行中（首轮 API 取证已记下；结论与根因待继续探查）

**返回：** [`README.md`](README.md)

---

## 1. 首轮取证（本地实例）

以下取自 **`GET /api/issues/ROU-44`**（约 2026-05-16 会话内；若实例数据变更请以当前 API 为准）。

### 1.1 识别字段（摘要）

| 字段 | 观测值 |
| --- | --- |
| `slug` / 可读键 | `ROU-44` |
| `kind` / 类型线索 | `stranded_issue_recovery`（实例 JSON 中出现该枚举语义） |
| 标题 | Recover missing next step ROU-41（英文原标题，照录） |
| 父级 / 关联链 | 指向 **ROU-41**（「recover」语义：补缺下一步） |
| 原因类标签（JSON） | `successful_run_missing_state`、`clear_next_step` 等（照 API） |
| `status` | `done` |
| `executionPolicy` / `executionState` | **均为 `null`** |
| 关联 run | `sourceRunId`：`cb4457cb-…`（全长以 DB/API 为准）；`correctiveRunId`：`d8da418a-…` |

### 1.2 异常线索

- 祖先 issue / 描述链路中曾出现 **中文乱码**（与评论编码类探查 **013～017** 可能共享根因；**未**在本单定论）。

---

## 2. 待查问题（ backlog ）

1. **控制面**何时生成 `stranded_issue_recovery`、与 `reconcileStrandedAssignedIssues` / issue graph liveness 的精确对应关系（代码路径 + 状态机）。  
2. **`successful_run_missing_state` + `clear_next_step`** 在同一工单上的业务语义：是否为「跑成功但未推进下一状态」类自动修复单。  
3. **ROU-41** 全链路与 **ROU-44** 关闭是否意味着收口完成，抑或仍存在旁路重复唤醒（对照活动日志）。  
4. 中文乱码出现的 **字段来源**（模板、导入、适配器 stdout、DB 编码）：是否与 **[015-评论乱码问题真因](015-探查-评论乱码问题真因.md)** 同一类。

---

## 3. 交叉引用

| 文档 | 用途 |
| --- | --- |
| [`018-探查-agent-pause-checkout-recovery链路与止损结论.md`](018-探查-agent-pause-checkout-recovery链路与止损结论.md) | pause / checkout / recovery 总链路 |
| [`012-探查-回形针任务运作机制-ROU49时间线.md`](012-探查-回形针任务运作机制-ROU49时间线.md) | 任务时间线写法示例 |
