[project]
# Allow Paperclip's domain to embed the Copilot widget cross-origin.
# In production, scope this to the Paperclip domain (e.g. ["https://admin.otto.nucleotto.com"]).
allow_origins = ["*"]

[run]
host = "0.0.0.0"
port = 8000
