EXPO_PUBLIC_PAPERCLIP_API_URL=http://100.92.162.66:3004
EXPO_PUBLIC_PAPERCLIP_COMPANY_ID=replace-with-company-id
EXPO_PUBLIC_PAPERCLIP_AGENT_ID=replace-with-agent-id
# local_trusted | authenticated
EXPO_PUBLIC_PAPERCLIP_DEPLOYMENT_MODE=authenticated
# Optional run context for diagnostics breadcrumbs (redacted in exported report).
EXPO_PUBLIC_PAPERCLIP_RUN_ID=
# Optional for local-only demos. Prefer pasting token in the app UI.
EXPO_PUBLIC_PAPERCLIP_API_KEY=
# Optional QA deterministic auth fixture.
# If set, app shows "Use QA fixture token" button for one-tap seeded login.
EXPO_PUBLIC_QA_SEEDED_API_KEY=
