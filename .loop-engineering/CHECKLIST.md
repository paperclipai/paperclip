# Kujo plugin host fixes

- [ ] Parse `@scope/name@version` before resolving the installed package directory.
- [ ] Preserve unscoped, scoped-unversioned, local-path, tag, range, and `--version` installs.
- [ ] Pass the detected Paperclip server version to plugin compatibility checks.
- [ ] Add regression tests for install parsing and host-version selection.
- [ ] Run targeted tests, typecheck, and build gates.
- [ ] Push an upstream pull request with the full Paperclip template.
