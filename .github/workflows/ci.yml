name: CI

on:
  pull_request:
    branches: ["main", "master"]
  push:
    branches: ["main", "master"]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect package manager
        id: pm
        shell: bash
        run: |
          if [ -f pnpm-lock.yaml ]; then
            echo "manager=pnpm" >> "$GITHUB_OUTPUT"
            echo "version=20" >> "$GITHUB_OUTPUT"
            pnpm_version="9"
            if [ -f package.json ]; then
              pm_line=$(grep -Eo '"packageManager"[[:space:]]*:[[:space:]]*"pnpm@[^"]+"' package.json || true)
              if [ -n "$pm_line" ]; then
                pnpm_version=${pm_line##*@}
                pnpm_version=${pnpm_version%\"}
              fi
            fi
            echo "pnpm_version=$pnpm_version" >> "$GITHUB_OUTPUT"
          elif [ -f package-lock.json ]; then
            echo "manager=npm" >> "$GITHUB_OUTPUT"
            echo "version=20" >> "$GITHUB_OUTPUT"
          elif [ -f yarn.lock ]; then
            echo "manager=yarn" >> "$GITHUB_OUTPUT"
            echo "version=20" >> "$GITHUB_OUTPUT"
          else
            echo "manager=npm" >> "$GITHUB_OUTPUT"
            echo "version=20" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ steps.pm.outputs.version }}

      - name: Setup pnpm
        if: steps.pm.outputs.manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: ${{ steps.pm.outputs.pnpm_version }}

      - name: Install dependencies
        shell: bash
        run: |
          case "${{ steps.pm.outputs.manager }}" in
            pnpm) pnpm install --frozen-lockfile ;;
            yarn) yarn install --frozen-lockfile ;;
            *) npm ci ;;
          esac

      - name: Lint
        shell: bash
        run: |
          case "${{ steps.pm.outputs.manager }}" in
            pnpm) pnpm run lint || true ;;
            yarn) yarn lint || true ;;
            *) npm run lint --if-present ;;
          esac

      - name: Typecheck
        shell: bash
        run: |
          case "${{ steps.pm.outputs.manager }}" in
            pnpm) pnpm run typecheck || true ;;
            yarn) yarn typecheck || true ;;
            *) npm run typecheck --if-present ;;
          esac

      - name: Test
        shell: bash
        run: |
          case "${{ steps.pm.outputs.manager }}" in
            pnpm) pnpm run test || true ;;
            yarn) yarn test || true ;;
            *) npm run test --if-present ;;
          esac

      - name: Build
        shell: bash
        run: |
          case "${{ steps.pm.outputs.manager }}" in
            pnpm) pnpm run build || true ;;
            yarn) yarn build || true ;;
            *) npm run build --if-present ;;
          esac
