name: Deploy Chase Telegram

on:
  push:
    branches:
      - master
    paths:
      - supabase/functions/chase-telegram/**
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref (branch, tag, or SHA) to deploy from
        required: false
        default: master
        type: string
      no-verify-jwt:
        description: Skip JWT verification
        required: false
        default: true
        type: boolean

concurrency:
  group: supabase-deploy-chase-telegram-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Set CHASE_AGENT_ID secret
        run: |
          supabase secrets set CHASE_AGENT_ID=717b8295-e4a5-4baf-98e5-e8c7d92d509b
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy chase-telegram function
        run: |
          supabase functions deploy chase-telegram \
            --project-ref tujyntcurpxvxgokcsaz \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
