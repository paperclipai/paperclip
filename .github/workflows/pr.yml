name: PR

on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Pin: #13300 merge — restore-only dependency caches and parallel native verification.
    uses: paperclipai/paperclip/.github/workflows/pr-trusted.yml@09e208c54fe1169fdd4182ac965c56182d5760ee
