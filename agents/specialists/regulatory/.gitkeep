# Specialist scaffold for regulatory
# Populate this directory with one Markdown file per specialist sub-agent (e.g., regulatory/foo-drafter.md).
# Each specialist defines: name, description, model, tools, inputs, outputs, gates_triggered.
