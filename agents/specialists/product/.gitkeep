# Specialist scaffold for product
# Populate this directory with one Markdown file per specialist sub-agent (e.g., product/foo-drafter.md).
# Each specialist defines: name, description, model, tools, inputs, outputs, gates_triggered.
