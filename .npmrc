auto-install-peers=true

# ARI-285 production fork preflight는 repo root에서 직접 import를 실행한다.
# CLI runtime은 bundle/server-local 경로를 쓰지만, cutover gate의 dry-run도 통과하도록 공개 hoist를 고정한다.
public-hoist-pattern[]=zod
public-hoist-pattern[]=postgres
public-hoist-pattern[]=tsx
public-hoist-pattern[]=@paperclipai/server
