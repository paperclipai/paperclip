auto-install-peers=true
# GitHub Actions supplies this from its protected NODE_AUTH_TOKEN secret for
# registry operations. Keep the token out of the repository and local config.
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
