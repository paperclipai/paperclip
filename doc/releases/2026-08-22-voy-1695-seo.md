# Release: SEO Metadata Infrastructure — VOY-1695 / VOY-1815

**Date:** 2026-08-23
**Release Engineer:** VOY-1798

## Scope
Add basic SEO infrastructure across the Paperclip app:
1. sitemap.xml generation (server-side, GET /sitemap.xml)
2. robots.txt (server-side, GET /robots.txt)
3. Meta description tags on all pages (usePageMeta hook)
4. Open Graph and Twitter Card tags on all public pages (VOY-1815)
5. Proper heading hierarchy (descoped — see review)

## Implementation
- New file: `server/src/routes/seo.ts` — sitemap.xml + robots.txt routes
- New file: `ui/src/hooks/usePageMeta.ts` — React hook for title + meta description + OG/Twitter tags
- Modified: `ui/index.html` — base OG/Twitter card defaults
- Modified: `server/src/app.ts` — register SEO routes before SPA fallback
- Modified: 75+ page components in `ui/src/pages/` — added usePageMeta() calls

## Key Decisions
- sitemap.xml returns empty `<urlset>` with 200 on DB error (prevents crawler retry storms)
- robots.txt generated dynamically with X-Forwarded-Host support
- Meta tags managed by last-call-wins effect (child routes override parent)
- OG/Twitter tags managed alongside description tags in the same hook
- Heading hierarchy audit descoped per Founding Engineer decision
- `isNull(issues.hiddenAt)` filter applied to exclude hidden issues from sitemap
- XML escaping applied to host, path, and lastmod to prevent injection via X-Forwarded-Host

## Commit History
- `1a50ce7446` — fix(VOY-1695): address code review findings from VOY-1696
- `fde711db21` — fix(VOY-1695): address Staff Engineer structural audit findings
- `c6ace14558` — feat(VOY-1695): SEO metadata infrastructure — sitemap.xml, robots.txt, per-page meta tags
- `a2ad8f8d90` — Release: Ship VOY-1798 SEO metadata infrastructure
- `c197a815b0` — docs(seo): commit v0.4.1 SEO metadata documentation
- `54aa4cb128` — docs(support): heartbeat — committed v0.4.1 SEO docs, board clean

## Review
- [x] Code Review (VOY-1696) — APPROVED
- [x] Structural Audit (Staff Engineer) — APPROVED
- [x] Docs Verification — Support Engineer
- [x] CTO Sign-off

## QA
- [x] sitemap.xml returns valid XML with company pages
- [x] robots.txt serves correct sitemap link
- [x] Meta tags render on all page components
- [x] Hidden issues excluded from sitemap
- [x] XML injection blocked via escapeXml
- [x] Open Graph tags render on all public pages
- [x] Twitter Card tags render on all public pages

## Status
- [x] Implementation (VOY-1695)
- [x] Code Review (VOY-1696)
- [x] CTO Sign-off
- [x] Ship to production