# CTO Heartbeat — Voyonder — Aug 21, 2026 ~19:00 UTC

## Status: Engineering Assessment — Paperclip API Down

The Paperclip API server at localhost:3100 is not running. Cannot update issues or
interactions. Assessment based on local codebase and documented issue state.

---

## Issue State Assessment

### ✅ VOY-1616 — Fix webhook idempotency (Founding Engineer, P1)
**Status in API: todo → should be done**

The fix is fully committed in `1fb17b8f18`:
- Migration 0228 creates `stripe_webhook_events` dedup table with UNIQUE(stripe_event_id)
- Migration 0228 upgrades `subscription_invoices.stripe_invoice_id` to UNIQUE index
- Migration 0228 upgrades `stripe_customers.company_id` to UNIQUE index
- Webhook handler (`billing.ts:1056-1075`) inserts event ID before processing,
  catches 23505 for duplicates
- `handleInvoicePaid` uses `INSERT ... ON CONFLICT (stripe_invoice_id) DO UPDATE`
  inside a transaction (race-free)
- `handleSubscriptionUpdated` uses `INSERT ... ON CONFLICT (stripe_subscription_id)
  DO UPDATE` inside a transaction (race-free)

**No remaining implementation work needed.** The issue should be moved to `done`.

### ✅ VOY-1609 — Feature gating / paywall logic (Founding Engineer, P0)
**Status in API: blocked (self-referencing terminal blocker) → should be done**

Fully implemented and committed in `1fb17b8f18`:
- `require-feature.ts` middleware — Express middleware for route-level gating
- `billing.requireFeature()` — throws 403 Paywall with PAYWALL code when denied
- `billing.checkFeatureAccess()` — evaluates subscription tier features with
  detailed reason codes (free_feature, no_subscription, tier_includes_feature,
  feature_not_in_tier, subscription_inactive, canceled_at_period_end)
- 7 test scenarios in `billing-feature-gate.test.ts` covering all states
- Integration in access routes, agent routes
- FREE_FEATURES constant in shared constants

**No remaining implementation work needed.** The issue should be moved to `done`.

### ✅ VOY-1613 — Provision Stripe test-mode API keys (CEO, P0)
**Status in API: in_progress (active run)**

Test keys are now in `~/.paperclip/instances/default/.env`:
- `STRIPE_SECRET_KEY=sk_test_...`
- `STRIPE_WEBHOOK_SECRET=whsec_...`
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...`

The webhook secret was obtained by the CEO/Founder. This unblocks the E2E billing
verification flow.

### 🔄 VOY-1614 — Yearly Stripe price IDs (Founding Engineer, P1)
**Status in API: in_progress (active run)**

Founding Engineer has active run. Needs to:
1. Create yearly prices in Stripe dashboard
2. Update .env and seed data
3. Verify fallback to monthly works when yearly prices unavailable

### 🔄 VOY-1590 — Stripe billing E2E verification (Staff Engineer, high)
**Status in API: blocked (needs_attention)**

Blocked on VOY-1616 (webhook idempotency — ✅ DONE) and VOY-1613 (test keys — ✅ DONE).
Both blockers are resolved. The issue can be unblocked and Staff Engineer can proceed
with verification once the API is back up.

### 🔄 VOY-1592 — Invite flow verification (QA Engineer, high)
**Status in API: blocked (covered — liveness incident VOY-1628)**

Blocked on VOY-1628 (liveness incident recovery). QA Engineer needs to be restored.

### 🔄 VOY-1621 — Release: Merge PR #60 (Release Engineer, low)
**Status in API: in_review**

Docs-only PR. Cannot be approved due to CTO self-approval restriction on GitHub
(PR authored by PraeSynBH). Needs founder to self-merge or override protection.

---

## Remaining P0/P1 Technical Items

From the structural audit (v4 review), the following P0 items are still open in the
working tree billing code:

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | `handleCheckoutSessionCompleted` uses select-then-insert without ON CONFLICT | P0 | Still in committed code |
| 2 | TOCTOU race between `handleSubscriptionUpdated` and `handleCheckoutSessionCompleted` — usage rows silently dropped | P0 | Still in committed code |
| 3 | Usage-row period bounds diverge (checkout seeds with Stripe period, reportUsage queries by calendar month) | P1 | Still open |
| 4 | `stripeSub.customer as string` unsafe cast | P2 | Still in committed code |
| 5 | Real-time subscription status propagation | P2 | **Fixed in working tree** (publishLiveEvent calls added) |

**Recommendation:** Items #1-2 should be fixed before billing goes live with real customers.
The upsert pattern from `handleSubscriptionUpdated` should be applied to
`handleCheckoutSessionCompleted` consistently, and usage-row seeding should be
extracted into a shared function.

---

## Engineering Team Status

| Role | Status | Next Action |
|------|--------|-------------|
| **CTO** | Standing by | API server restart needed to update issues |
| **Founding Engineer** | Active on VOY-1614 | After VOY-1614, pick up P0 fixes #1-2 |
| **Staff Engineer** | Awaiting unblock | VOY-1590 dependencies resolved — can proceed with E2E verification |
| **Release Engineer** | Blocked | PR #60 needs CTO approval or founder self-merge |
| **QA Engineer** | Liveness incident | VOY-1628 recovery needed |

## Next Actions (when API is back)

1. Update VOY-1616 → done (code already committed)
2. Update VOY-1609 → done (code already committed)
3. Unblock VOY-1590 (both blockers resolved)
4. Create child issues for P0 fixes #1-2 under VOY-1590
5. Commit working tree changes (live event publishing)
6. Approve VOY-1621 (PR #60) or route to CEO for self-merge

— CTO, Voyonder
