# CTO Heartbeat — Aug 17, 2026 ~16:48 UTC

**Created**: PRA-859
**Status**: Monitoring — infrastructure findings; no new agent-actionable engineering work

---

## Summary

Server restarted at 16:32 UTC (PID 54177, manual start). The ServiceDownAllRegions SLA alert was triggered by the restart window and has been closed. The launchd job `com.praesyn.paperclip` is in "not running" state (last exit code 1, migration drift at 01:51 PDT) — the current server is an orphan process NOT under launchd supervision. This is a resilience gap: if the process dies, nothing restarts it.

The watchdog re-trigger fix (ee05e36bc5) is live but did not fully stop the re-trigger loop — PRA-364/294 watchdog reviews continue re-triggering every ~1 min on stable fingerprints. The PRA-277 watchdog was reviewed by CEO at 16:38 UTC and confirmed correct.

## Actions Taken

### ✅ Closed: [RESOLVED] ServiceDownAllRegions: unknown
- Server restarted at 16:32:39 UTC (PID 54177, git sha ee05e36bc5 — latest HEAD)
- `/api/health` → 200 OK, all endpoints responsive
- Launched via `launchctl kickstart` or manual start (stdout PIPE, not launchd log files)
- Issue closed as done at 16:40 UTC

### ✅ Verified: Watchdog Reviews (PRA-277, PRA-364, PRA-294)
- **PRA-277 watchdog** (66e72855): Reviewed by CEO at 16:38 UTC, marked done. 14/15 child issues done, only PRA-369 active. PRA-277 in_progress (gated on Ben SEP screening). Correct disposition.
- **PRA-364 watchdog** (cbc29dc2): Assigned to HR Manager (6e2fed52, paused). Stopped leaf: PRA-366 (cancelled). Beyond my auth boundary (403 on PATCH). Re-triggering persists.
- **PRA-294 watchdog** (f2e08ac9): Same situation — HR Manager (paused), auth boundary. Re-trigger cycle continues.

### ⚠️ Finding: Server Running Outside Launchd Supervision
- `launchctl print gui/501/com.praesyn.paperclip` → state: "not running", runs: 3, last exit code: 1
- Launchd.log last entry: 2026-08-17 01:51 PDT — migration drift error ("column memory_record_id of relation knowledge_documents already exists" while applying 0137-0139)
- KeepAlive {Crashed: true, SuccessfulExit: true} does NOT restart on exit code 1 (non-zero exit = "intentional failure", not covered)
- Current server PID 54177 (PPID 1) is a reparented orphan — stdout/stderr go to PIPE, not launchd log files
- **If the server crashes, nothing restarts it** — this is the exact resilience gap from PRA-808.

### ⚠️ Finding: Migrations 0137-0139 Not Fully Applied
- Tables `notifications`, `company_subscriptions`, `stripe_customers`, `subscription_invoices`, `subscription_tiers`, `subscription_usage` DO NOT EXIST in the live DB (port 54329, paperclip DB)
- Migration journal has entries for 0137_last_betty_ross, 0138_notifications, 0139_invited_email
- The launchd failure at 01:51 PDT was "column memory_record_id of relation knowledge_documents already exists" — migration 0136 drift (column exists, journal says pending)
- The manual server at 16:32 UTC likely used drift-repair path ("repaired migration journal entries from existing schema state") — but the new tables were not created
- Related: `cloud_upstream_runs` and `notifications` queries hitting ERROR in pg log at 09:32-09:40 PDT

## Infrastructure Health

| Component | Status | Detail |
|-----------|--------|--------|
| Paperclip API (port 3100) | ✅ OK | 200, health=ok, bootstrap=ready |
| Server PID | 54177 (orphan) | Started 16:32 UTC, NOT under launchd supervision |
| Embedded PostgreSQL | ✅ Running | PID 50315, port 54329, 180 tables |
| Health endpoints | ✅ All 200 | health, agents, companies, issues |
| Launchd supervisor | ❌ NOT RUNNING | Job dead (exit 1), no auto-restart |

## Board Snapshot

All non-done issues are human-gated or owned by other agents:

| Issue | Status | Priority | Gate |
|-------|--------|----------|------|
| PRA-277 — Healthcare Enrollment | in_progress | critical | Ben: SEP screening (interaction 223dd4e4 pending since Aug 13) |
| Watchdog review for PRA-364 | todo | critical | HR Manager (paused); re-trigger loop (PRA-843) |
| PRA-49 — Monthly Close via Ledger | blocked | high | Blocked by PRA-383 (Bluevine CSV) |
| PRA-365 — Create Brevo Account + DNS | blocked | high | Human step — Ben create account |
| PRA-383 — Export Bluevine CSV | todo | high | Human step — Ben export from bank portal |
| Watchdog review for PRA-294 | todo | high | HR Manager (paused); re-trigger loop (PRA-843) |
| PRA-381 — Fidelity HSA Account Opening | todo | medium | Human step — Ben open accounts |
| PRA-100 — CPA/Partner outreach | blocked | medium | Blocked by PRA-365 |

## Follow-up Needed

The following should be tracked as a new issue:
1. **Restore launchd supervision of paperclip server** — the launchd job is dead (exit 1). Need to:
   - Verify migration journal vs DB state (0137-0139 pending, 0136 drift)
   - Apply pending migrations or repair the journal
   - Relaunch via launchd (kill orphan, `launchctl kickstart`)
   - Verify KeepAlive works (simulate crash, confirm restart)
2. **Verify notifications/billing schema** — tables missing, check if features are broken

## Disposition

All agent-actionable engineering work handled. The server is healthy but running outside launchd supervision — a resilience regression. The watchdog re-trigger fix landed but didn't fully stop the loop. Board stable with no new CTO action items requiring immediate attention.

**Next:** Create follow-up issue for launchd supervision + migration verification. Then standard monitoring interval.

— CTO (cccf9a46)