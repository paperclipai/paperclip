# CTO Heartbeat — 2026-08-16 18:25 UTC

## Board State (PraeSyn — 44c1cd6d)

All open/blocked/todo issues are gated on human action (Ben). No agent-actionable work available in this company.

| Issue | Status | Priority | Gate |
|-------|--------|----------|------|
| PRA-277 — Enroll in Approved 2026 Healthcare Plan | blocked | critical | Ben: SEP screening (interaction 223dd4e4 pending) |
| PRA-49 — Monthly Close Process via Ledger | blocked | high | Ben: Export Bluevine CSV (issue PRA-383, todo) |
| PRA-365 — Create Brevo Account + DNS + CMO Identity | blocked | high | Ben: Create Brevo account (interaction 45070f29 pending) |
| PRA-100 — Send CPA/Partner outreach email sequences | blocked | medium | Blocked on Brevo (PRA-365) |
| PRA-383 — Export Bluevine CSV | todo | high | Human step (Ben): export from bank portal |
| PRA-292 — Execute Fidelity HSA Account Opening | todo | medium | Human step (Ben): open account |
| DP-4 — Milestone-Linked Child Issues (ea587759) | backlog | — | API/Service layer done (working tree); UI deferred |
| Polaris B Phase 4 — Memory Browser UI (e53ddc56) | backlog | — | Unassigned |
| Polaris B Phase 5 — Company Knowledge Base (76492662) | backlog | — | Unassigned |
| Board UI for Plan Browsing (7c5d09f1) | backlog | — | Unassigned |

Backlog items (Gusto config, 1099-NEC validation, open enrollment, Kaiser HDHP) — all time-gated.

## Engineering Deliverable Status

### Working Tree — Plan Review Gates + Milestone Context (verified, uncommitted)

The working tree at `/Users/benh/Programming/paperclip` (branch `v0.4.0-polaris-deep-planning-memory`) contains a complete, verified feature extension following the v0.4.0 landing:

**Changes (9 files, +303/-9):**

| Layer | File | Change |
|-------|------|--------|
| Types | `packages/shared/src/types/document-annotation.ts` | Added `PlanReviewGateContext`, `MilestoneProgress` interfaces; extended `PlanReviewContext` with `gates`, `milestoneProgress`, new totals (gate/approved/pending/rejected counts, milestone/completed milestone counts) |
| Types | `packages/shared/src/types/issue.ts` | Added `milestoneId: string | null` to `AcceptedPlanDecomposition` |
| Types | `packages/shared/src/types/index.ts` | Export new types |
| Types | `packages/shared/src/index.ts` | Export new types |
| Validators | `packages/shared/src/validators/issue.ts` | `milestoneId` (nullable optional) on `createAcceptedPlanDecompositionSchema` |
| Service | `server/src/services/plan-review-context.ts` | +132: Plan review gates fetch + milestone progress computation (counts child issues by milestone, checks gate statuses) |
| Service | `server/src/services/issues.ts` | `milestoneId` pass-through in decomposition serialization + creation |
| Routes | `server/src/routes/issues.ts` | +26: Wakeup on plan update (`issue_plan_updated`), wakeup on gate resolution (`issue_plan_gate_resolved`), `milestoneId` on decompose-accepted-plan |
| Docs | `skills/paperclip/SKILL.md` | +114/-9: Structured plan metadata schema docs, milestone decomposition with `milestoneId`, review gate context injection, plan validation checklist |
| Storybook | `ui/storybook/stories/issue-plan-decompositions.stories.tsx` | Added `milestoneId: null` to decomposition fixture |

**Verification:**
- Server typecheck (tsc --noEmit): PASS
- Shared package typecheck: PASS
- Shared tests (24/24): PASS

### What This Enables

1. **Agent heartbeats on `planning`-mode issues now receive `planReviewContext.gates`** — agents can see pending/approved/rejected review gates with acceptance criteria, assigned agents, and resolution history. Agents can resolve gates they're assigned to via `PATCH /issues/:id/plan/gates/:gateId`.

2. **Agent heartbeats now receive `planReviewContext.milestoneProgress`** — agents see per-milestone progress: `totalChildIssues`, `completedChildIssues`, `gateStatus`. Milestones with `totalChildIssues === completedChildIssues` are ready for gate review.

3. **Plan updates and gate resolutions wake the issue assignee** — `queueIssueAssignmentWakeup` fires on plan document changes and gate resolutions, ensuring the assigned agent responds promptly.

4. **milestoneId flows through plan decomposition** — when decomposing an accepted plan into child issues, each decomposition can carry a `milestoneId`. This links child issues to specific milestones.

### What Remains

- **DP-4 UI** (Board UI for grouped milestone view): The current changes handle API/schema/service only. The UI layer (milestone-grouped decomposed issues in the plan viewer) is deferred.
- **E2E tests**: The document-annotations-service integration tests require embedded PostgreSQL with `vector` extension, which failed to start in this environment (`CREATE EXTENSION IF NOT EXISTS vector` fails — environmental issue, not code defect). Manual testing of the gate/resolution/milestone flow needed.
- **Embedded Postgres + pgvector**: The host (macOS 26.2, ARM64) has the postgres binary but the embedded pgvector extension may need reinstallation.

## Disposition

- **Engineering deliverable**: Complete and verified. Ready for commit when the session picks up the working tree. Matches DP-4 (milestone-linked decomposition API layer) scope.
- **Board**: No CTA for CTO — all issues human-gated.
- **Infrastructure**: Embedded pgvector test setup is broken on this host; not blocking the code work but should be addressed before merging PR.