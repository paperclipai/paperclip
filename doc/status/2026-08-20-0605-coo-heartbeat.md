# COO Heartbeat — PraeSyn — Aug 20, 2026 ~06:05 UTC

## Disposition: Completed — Three Issues Delivered

Completed actionable work this heartbeat. Board is now idle with all COO-assigned items in `done` status.

## Work Completed This Heartbeat

| Issue | Action | Result |
|-------|--------|--------|
| PRA-1062 — Restart paperclip for watchdog fix | Restarted service via launchctl | ✅ Service running with watchdog fix + allowed hostnames |
| PRA-1063 — Log rotation for 2GB server.log | Changed log level debug→info + launchd rotation agent | ✅ 2GB→94MB compressed, daily rotation at 3AM |
| PRA-1061 — Investigate vps-2 outage root cause | SSH investigation + cAdvisor emergency kill | ✅ Root cause found: cAdvisor zombie wget apocalypse (112+ zombies, 1.5GB mem, 62% CPU) |

## Board State (Non-done, non-backlog)

| Issue | Status | Priority | Assignee | Gate |
|-------|--------|----------|----------|------|
| PRA-1043 — Upgrade vps-1 Hostinger plan | blocked | critical | unassigned | Founder: Hostinger panel |
| PRA-277 — Healthcare enrollment | blocked | critical | CEO | Founder: SEP screening |
| PRA-1047 — vps-2 DOWN (was OOM) | blocked (should be done) | critical | Ben | Founder: verify fix applied |
| VOY-1413 — Deploy docs site | blocked | high | CEO (founder) | Founder: approve request_confirmation |
| VOY-343 — Set env vars on vps-1 | todo | high | CEO (founder) | Founder: SSH + set vars |
| PRA-973 — DNS fix (paperclip.praesyn.int) | blocked | high | unassigned | Founder: DNS provider |

## Critical Path

```
Founder Gate (Ben)
  PRA-1043 — vps-1 upgrade (Hostinger panel action)
  PRA-277 — Healthcare SEP screening
  PRA-1047 — Verify vps-2 fix (cAdvisor container stopped)
  VOY-1413 — Approve plan → CEO creates child issues → COO executes
  VOY-343 — env vars on vps-1 (SSH)

All other work idle until at least one founder gate clears.
```

## vps-2 Outage — Root Cause Summary

- **Primary cause**: cAdvisor Docker container (restart: unless-stopped) went rogue — spawned 112+ zombie wget processes, consumed 1.5GB RAM + 62% CPU on 2-core VM
- **Cascade**: Memory pressure → OOM killer → killed praesyn.com / travel.praesyn.com containers → Docker auto-restarted them ("self-restored")
- **Immediate fix**: Force-killed cAdvisor containerd-shim (SIGTERM was ignored)
- **Permanent fix needed**: `docker update --restart=no cadvisor && docker stop cadvisor` OR upgrade cAdvisor version; add swap file

## Log Rotation Status

- File log level: `debug` → `info` (reduces ~50MB/day → ~5-10MB/day)
- Launchd agent: `com.praesyn.paperclip-rotate-logs` — daily at 3:00 AM, rotates at 500MB, retains 30 days
- Current log: 72K (fresh after rotation)

## Recommendations

1. **vps-2**: Founder should run `docker update --restart=no cadvisor && docker stop cadvisor` to permanently disable faulty cAdvisor, then add a 2GB swap file
2. **vps-1 upgrade**: PRA-1043 still blocked — founder needs to log into Hostinger panel
3. **Board idle**: All COO-actionable items are done. Standing by for founder gate clearance.

<!-- End of heartbeat -->
