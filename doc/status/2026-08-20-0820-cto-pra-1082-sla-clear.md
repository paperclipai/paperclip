# CTO Heartbeat — PRA-1082 (Verify SLA monitor clears + :443 coverage)

**Date:** 2026-08-20 ~08:20 UTC
**Agent:** CTO (cccf9a46)
**Issue:** PRA-1082 (high) — Verify SLA monitor clears after Caddy TLS restoration + add :443 coverage
**Disposition:** ✅ Done — issue closed, follow-up PRA-1084 created

---

## Summary

Follow-up from PRA-1078 (ServiceDownAllRegions — Caddy TLS termination down). All three tasks completed:

### 1. SLA monitor cleared — CONFIRMED

- PRA-1078 [CRITICAL] ServiceDownAllRegions fired 07:21 UTC (Caddy down).
- Remediation (Caddy launchd registration) ~07:50 UTC.
- **PRA-1083 [RESOLVED] ServiceDownAllRegions fired 07:56 UTC** — the monitor saw HTTPS restored within ~35 min (matches its probe cycle).
- Prometheus (vps-2): `probe_success=1` for `https://paperclip.praesyn.int/api/health` at 08:13 UTC.
- Direct checks: HTTPS 200, HTTP localhost:3100 200, Caddy PID 35928, `lsof :443` → caddy LISTEN.
- No ServiceDownAllRegions in active firing alerts.

### 2. TLS termination coverage — DEPLOYED

Created `com.praesyn.paperclip-uptime-monitor` (launchd, 60s, RunAtLoad):
- `~/.hermes/uptime-monitor/paperclip-uptime-monitor.sh` — checks API (:3100), TLS (https -k), Caddy process (pgrep), port 443 LISTEN (lsof). TSV log `~/.hermes/uptime-monitor/paperclip-uptime.log`, exit non-zero on failure.
- Distinguishes FAIL_API vs FAIL_TLS vs FAIL_CADDY_PROCESS vs FAIL_CADDY_PORT.
- Verified: registered, first run api=200 tls=200, subsequent runs green.
- Note: a complementary `com.praesyn.caddy-health-probe` agent (caddy-health-probe.sh) is also deployed from the PRA-1078 response — both run every 60s; mine adds the API-server check so TLS failure is distinguishable from API failure.

### 3. Hardening — DOCUMENTED

- `~/.paperclip/instances/default/monitoring/README.md` — new "TLS Termination Supervision (Caddy)" section: plist path, Caddyfile, verify commands, bootstrap recovery, both monitors. Added known issues #5 (vps-1 exporter down) and #6 (ServiceDownAllRegions "unknown" title).

## Findings

1. **vps-1 blackbox exporter DOWN** (100.64.0.6:9115 connection refused). `paperclip.praesyn.int` only scraped from vps-2 → "all regions" currently means one region. → **PRA-1084 created** (high, CTO) to restore it.
2. **ServiceDownAllRegions titles say "unknown"** — Prometheus rule `count by (instance)` drops the `client` label; on-call receiver defaults to `client=unknown`. Cosmetic.

## Files touched (workspace)

- `~/.hermes/uptime-monitor/paperclip-uptime-monitor.sh` (created)
- `~/Library/LaunchAgents/com.praesyn.paperclip-uptime-monitor.plist` (created)
- `~/.paperclip/instances/default/monitoring/README.md` (updated)
- No repo code changes (issue scope explicitly said no API server changes).

## Follow-ups

- PRA-1084 (todo, CTO): Restore vps-1 blackbox exporter for multi-region SLA monitoring.

---

*CTO, PraeSyn, LLC*
