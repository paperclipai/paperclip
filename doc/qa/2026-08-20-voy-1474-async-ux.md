# QA Verification Report — VOY-1474 (M1+M2) Async UX Post-Deploy

**Date:** 2026-08-20
**QA Engineer:** Voyonder QA
**Issue:** VOY-1496 — QA verify: VOY-1474 async UX post-deploy
**Verdict: PASS** ✅

---

## Verification Scope

Verify the async UX pipeline end-to-end:

- Activity search returns immediately with `jobId`
- `autoAssess` runs as a background job
- Keyword-first search returns synchronously + optional async semantic upgrade via `semanticJobId` / SSE
- PDF/ICS export runs as a background job with status tracking
- `BackgroundProcessTray` shows active jobs
- `StatusCue` shows inline progress
- `IncompleteDataNotice` shows during job run
- `FreshnessCue` indicates staleness
- Skeleton loading + FadeIn for non-blocking data

## Verification Methods

1. **Static code review** — every route handler, service, and UI component examined against spec.
2. **Integration test execution** — 31 targeted tests run and passing (embedded Postgres).
3. **Type checking** — server `tsc --noEmit` passes.
4. **Release engineer route verification** (from deployment at 17:22 UTC, VOY-1495):
   - `POST /research/activities` → 202 `{ jobId }`
   - `POST /research/auto-assess` → 202 `{ jobId }`
   - `POST /research/search` → keyword results + `semanticJobId`
   - `POST /exports/pdf` / `POST /exports/ics` → 202 `{ jobId }`
   - `POST /exports/pdf` (>512KB) → 413
   - `GET /background-jobs` → 200 (paginated, filterable)
   - `GET /background-jobs/:id` → 200 or 404
   - `GET /background-jobs/events` → SSE stream

## Test Results

| Test Suite | Tests | Status |
|---|---|---|
| `background-jobs-service.test.ts` | 14 | ✅ ALL PASS |
| `research-search-service.test.ts` | 12 | ✅ ALL PASS |
| `escape-probe.test.ts` | 5 | ✅ ALL PASS |
| **Total** | **31** | **✅ ALL PASS** |

## Feature-by-Feature Verification

| # | Feature | Assertion | Evidence | Status |
|---|---|---|---|---|
| 1 | `POST /research/activities` | 202 + `jobId` immediately | `server/src/routes/research.ts:69` | ✅ PASS |
| 2 | `POST /research/auto-assess` | 202 + `jobId` immediately | `server/src/routes/research.ts:95` | ✅ PASS |
| 3 | `POST /research/search` | Keyword results synchronously + `semanticJobId` | `server/src/routes/research.ts:110-133` | ✅ PASS |
| 4 | `POST /exports/pdf` | 202 + `jobId`; 413 on oversized payload | `server/src/routes/exports.ts:82`, `:38-45` | ✅ PASS |
| 5 | `POST /exports/ics` | 202 + `jobId`; 413 on oversized payload | `server/src/routes/exports.ts:111`, `:38-45` | ✅ PASS |
| 6 | Background job worker | All 5 processors (activity_search, semantic_search, auto_assess, pdf, ics) | `server/src/services/background-job-worker.ts:55-198` | ✅ PASS |
| 7 | Worker retry | Exponential backoff (2 retries, 1s/2s/4s capped at 30s) | `background-job-worker.ts:321-332` | ✅ PASS |
| 8 | Worker timeout | `Promise.race` 5-min processor timeout | `background-job-worker.ts:274-297` | ✅ PASS |
| 9 | Worker atomic claim | `FOR UPDATE SKIP LOCKED` + update in one transaction | `background-job-worker.ts:209-242` | ✅ PASS |
| 10 | SSE events stream | Pushes `background_job.status` events | `server/src/routes/background-jobs.ts:46-71` | ✅ PASS |
| 11 | SSE authz | `assertCompanyScopeReadAllowed` enforced | `routes/background-jobs.ts:50` | ✅ PASS |
| 12 | `useJobStatus` hook | 2s polling + optional SSE; stops at terminal | `ui/src/hooks/useJobStatus.ts:67-157` | ✅ PASS |
| 13 | `StatusCue` | Colored dot, label, progress bar, error text | `ui/src/components/ui/StatusCue.tsx:42-99` | ✅ PASS |
| 14 | `IncompleteDataNotice` | Banner while queued/running; hides when done | `ui/src/components/ui/IncompleteDataNotice.tsx:17-47` | ✅ PASS |
| 15 | `ActivitySearchPanel` | Search + scope + job status + results callback | `ui/src/components/ActivitySearchPanel.tsx:33-163` | ✅ PASS |
| 16 | `BackgroundProcessTray` | SSE + 5s polling fallback; running-on-top; progress bars; duration | `ui/src/components/BackgroundProcessTray.tsx:29-254` | ✅ PASS |
| 17 | `FreshnessCue` | Green (≤7d), amber (≤30d), grey (>30d) | `ui/src/components/ui/FreshnessCue.tsx:1-103` | ✅ PASS |
| 18 | `SkeletonBone`/`SkeletonText` | Animated pulse placeholders | `ui/src/components/ui/FadeIn.tsx:1-99` | ✅ PASS |
| 19 | `FadeIn` wrapper | Opacity transition on mount | `ui/src/components/ui/FadeIn.tsx` | ✅ PASS |

## Gaps / Observations

1. **No route-layer API tests yet** — service-layer tests (14) cover CRUD + worker dispatch, but no HTTP-level supertest coverage. Release engineer verified routes manually. Known finding C6 from the M2 structural audit.
2. **No SSE end-to-end test** — SSE authz covered by unit test; no test creates a job and asserts the SSE event arrives.
3. **UI typecheck** — server typecheck passes; UI typecheck confirmed at deploy (UI rebuilt with new components per VOY-1495).
4. **Live instance unreachable from QA environment** — browser-based E2E could not be executed here; route contracts were verified at deployment time instead.

## Verdict

**QA: PASS** ✅ — All 31 backend tests pass. All feature implementations verified against spec by code inspection. Route contracts confirmed during deployment. No regressions detected.
