**Title:** Fixes That Cut Your CI Build from Minutes to Seconds **Issue:** TSM-5973 | **Date:** 2026-08-01 | **Audience:** Working software engineers, mid-level **Target runtime:** minutes | **Voice:** Charon (Sam) **Pacing notation:** authored silence; TTS must honour these do not strip **Pronunciation notation:** `(pr: IPA-free phonetic guide for the TTS pronunciation dictionary --- | Term | Correct | Wrong (flag/reject) | |---|---|---| | Docker | "dock-er" | "dook-er", "dock-ur" | | Dockerfile | "dock-er file" | "dook-er file" | | npm | "N-P-M" (three letters) | "num", "num-pee-em" | | YAML | "yam-ul" | "ya-mel", "yam-ul" | | SHA | "shah" (one syllable) | "shaw", "shuh" | | package-lock.json | "package lock dot jay-son" | "package lock dot json" | | BuildKit | "build-kit" | "build-keet" | | CI | "C-I" (two letters) | "see-eye" | | ghcr.io | "G-H-C-R dot eye-oh" | "gee-hee-see-ar" | | devDependencies | "dev deh-pen-den-seez" | | | hashFiles | "hash files" | "hash-files" | | lockfile | "lock file" | | | GitHub Actions | "git-hub ack-shuns" | | | COPY (Dockerfile instruction) | "copy" | | | restore-keys | "restore keys" | | | ubuntu-latest | "oo-bun-too lay-test" | | --- --- Every push you make kicks off this whole pipeline. The real question is how long are you sitting here waiting for it? Eight minutes. Forty-two seconds. That is how long this build takes, on every single push. Node project, three-stage GitHub Actions (pr: "git-hub ack-shuns") pipeline, one Docker (pr: "dock-er") image. Numbers you will see on screen exactly as I say them. By the end of this, three changes nothing you need to install, nothing new to maintain cut that to seventy-two seconds. Let me show you where the time goes, and what you change to stop paying for it. --- Here is the GitHub Actions (pr: "git-hub ack-shuns") timeline for that build. Three stages. npm (pr: "N-P-M") install: two minutes, seventeen seconds. Docker (pr: "dock-er") build: five minutes, nine seconds. Test run: one minute, sixteen seconds. The npm (pr: "N-P-M") install and the Docker (pr: "dock-er") build together eat seven and a half minutes of that eight forty-two. Those are also the two that did not need to run at full cost. Here is why. Your CI runner has no memory between builds. It does not know your package-lock.json (pr: "package lock dot jay-son") has not changed since last Thursday. It does not know that eleven of your twelve Docker (pr: "dock-er") layers are byte-for-byte identical to the last commit. It starts from scratch every time even when scratch means identical to what it did three minutes ago. And here is the thing about that: on a typical feature branch workflow, maybe twenty percent of your pushes actually touch a dependency. The other eighty percent are paying the full install cost for no reason at all. --- Fix one is the GitHub Actions (pr: "git-hub ack-shuns") cache, keyed to your lockfile (pr: "lock file"). Here is the YAML (pr: "yam-ul") most teams start with: The cache key is tied to the commit SHA (pr: "shah"). That means every commit even a one-line comment change busts the cache. You are downloading the same two hundred and forty packages from the npm (pr: "N-P-M") registry, on every push, regardless of whether anything changed. Here is the fix. Four lines, already in your workflow file: The key is now the hash of your package-lock.json (pr: "package lock dot jay-son"). If that file has not changed and in eighty percent of pushes, it has not you get a full cache hit. npm (pr: "N-P-M") install drops from two minutes seventeen to about four seconds. The restore-keys (pr: "restore keys") line is the fallback. If your lockfile (pr: "lock file") did change, you still restore the closest previous cache and install only the delta not every package from scratch. On the timeline: fix one alone brings the pipeline from eight forty-two to six twenty-five on any push that does not touch a dependency. That is eighty percent of your pushes running close to two minutes faster. --- Fix two is Dockerfile (pr: "dock-er file") layer ordering. This is where the Docker (pr: "dock-er") build time drops by about seventy percent. Here is the Dockerfile (pr: "dock-er file") most people write first: Docker (pr: "dock-er") builds in layers. Every COPY (pr: "copy") and RUN is a layer. When any file in the layer changes, Docker (pr: "dock-er") invalidates that layer and every layer below it. That COPY (pr: "copy") dot dot line copies your entire project directory. A one-line change to a source file one character touches that layer. Which invalidates the npm (pr: "N-P-M") install layer below it. Which rebuilds all twelve layers from scratch. The fix is to copy only what npm (pr: "N-P-M") needs first, install dependencies, then copy your source code: Now when you change a source file, Docker (pr: "dock-er") replays from that second COPY (pr: "copy") it skips the npm (pr: "N-P-M") install layer entirely because nothing above it changed. The build log goes from every step rebuilding, to this: Step four of seven: RUN npm install CACHED. Zero point zero seconds. On the timeline: five minutes nine in Docker (pr: "dock-er") build becomes one minute twenty. Combined with fix one, you are now at two minutes fifty-five, for eighty percent of pushes. --- Fix three is build once, use everywhere. Most multi-job pipelines have a hidden tax. The test job triggers a docker build. The deploy job triggers its own docker build. That is the same five minutes paid twice or three times, if you have a separate lint job doing the same thing. Here is the fix: add a build job that builds and pushes the image once. Test and deploy pull that same image tag they do not rebuild: Build once. Pull twice. Test and deploy run on exactly the same artifact. And you get a consistency guarantee for free the image that passes your tests is the image that gets deployed. On the wall-clock: two jobs that each took over five minutes now each take twelve seconds to pull the already-built image. The full pipeline build, test, deploy in sequence lands at one minute twelve. --- "If this is useful there is a one-page checklist for every fix in this video linked in the description. Takes two minutes to check against your own pipeline." --- Three quick additions that compound on these three fixes. First: BuildKit (pr: "build-kit"). Docker (pr: "dock-er") BuildKit (pr: "build-kit") is on by default since Docker (pr: "dock-er") and it gives you parallel layer builds. If your Dockerfile (pr: "dock-er file") has independent build stages a separate assets stage and a server stage they run concurrently. If you are on an older version, DOCKER_BUILDKIT equals one in your environment enables it today. Second: multi-stage builds. Your build stage does not need to ship with your source code, your devDependencies (pr: "dev deh-pen-den-seez"), or your compiler. A two-stage Dockerfile (pr: "dock-er file") that builds in one stage and copies only the output to a lean runtime image can cut your final image from eight hundred megabytes to sixty. Smaller image: faster push to the registry, faster pull in CI, and a smaller attack surface in production. Third: how to find your own bottleneck. In GitHub Actions (pr: "git-hub ack-shuns"), click into any run, expand the job, and look at the step durations. Sort by slowest. The longest step is your fix-one target. Apply the fix, run again, and repeat the process. You are looking for the step where the duration drops by ninety percent on the second run that is the cache working. --- Before this: eight minutes, forty-two seconds. Every push. Every developer on the team. Three changes: Fix one cache by lockfile (pr: "lock file"), not by commit. Fix two order your Dockerfile (pr: "dock-er file") stable to volatile. Fix three build your artifact once and pull it downstream. After: one minute, twelve seconds. Same pipeline. Same tests. Same team. Seven times faster, with nothing new to maintain. If your build is over ten minutes, the same three fixes apply the savings scale with the problem. If you are already under two minutes, look at test parallelism next different bottleneck, different playbook. --- "The CI cache checklist is in the description one page, three fixes, a column for your before and after numbers." --- --- **Word count:** words **Estimated runtime at Charon TTS pace wpm):** minutes (including pauses) **CTA slots:** mid-video at lead magnet at **Pause markup:** present throughout TTS must render these as real silence, not strip them
